/**
 * @module @commise/features-recipes/collections — Tailwind class strings shared by MORE THAN ONE web recipe-picker
 * body (`CollectionRecipePickerLoadError`, `CollectionRecipePickerCandidates`). A class string used by exactly one
 * body lives in that body's own file instead.
 *
 * The native counterpart is `collectionRecipePickerStyles.native.ts`.
 */

/** The white state card a settled failure, an empty library and an empty search all render on. */
export const pickerStateCard = 'rounded-2xl bg-card p-6 text-body-md text-slate shadow-sm';
