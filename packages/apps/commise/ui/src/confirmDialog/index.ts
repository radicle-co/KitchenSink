/**
 * @module @commise/ui/confirm-dialog — platform-neutral barrel for the design-system ConfirmDialog (house
 * pattern B6). The component specifier resolves to its web (`ConfirmDialog.tsx`) or native
 * (`ConfirmDialog.native.tsx`) leaf at bundle time; the prop contract is platform-agnostic. Consumed as
 * `@commise/ui/confirm-dialog`.
 */
export { ConfirmDialog } from './ConfirmDialog.js';
export type { ConfirmDialogProps } from './props.js';
