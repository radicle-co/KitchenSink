/**
 * @module @commise/ui/load-more — package export for the design-system "Load more" control. The component specifier
 * resolves to its web (`LoadMoreControl.tsx`) or native (`LoadMoreControl.native.tsx`) leaf at bundle time.
 * Consumed as `@commise/ui/load-more`.
 */
export { LoadMoreControl } from './LoadMoreControl.js';
export type { LoadMoreControlLabels, LoadMoreControlProps } from './props.js';
