/**
 * @module @commise/ui/offline-notice — the package export for the offline read slot.
 *
 * A barrel ONLY because it is the target of a `package.json` `exports` entry (the repo's one sanctioned use).
 */
export { OfflineReadSlot } from './OfflineReadSlot.js';
export type { OfflineReadSlotProps } from './props.js';
