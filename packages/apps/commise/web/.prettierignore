playwright-report
test-results
.next
node_modules
dist
coverage
*.tsbuildinfo
cdk.out
cdk.context.json
router/dist

# Vitest temp root (see packages/tools/vitest/testTempRoot.js). Gitignored scratch, incl. cdk.out<rand>.
.tmp-test

# Playwright storage state, one file per local e2e run (tests/e2e/auth.setup.ts). Gitignored scratch.
.auth
