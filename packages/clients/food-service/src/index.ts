/**
 * `@kitchensink/food-service-client` — typed client for our own `/v1/foods/*` API.
 *
 * Placeholder scaffold (T-060). The full client implementation — `getFood`/`searchFoods`/batch
 * with Clerk user-session or M2M token attachment and `401`/`403`/`400`/`503`/`404`/`202`
 * mapping — is deferred to T-057 in a later phase.
 */
export {};
