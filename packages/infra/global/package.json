{
    "name": "@kitchensink/infra-global",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "eslint 'lib/**/*.ts'",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "bundle:lambda": "node esbuild.mjs",
        "synth": "cdk synth --app 'npx tsx bin/app.ts'",
        "deploy": "npm run bundle:lambda && cdk deploy --app 'npx tsx bin/app.ts'"
    },
    "devDependencies": {
        "@kitchensink/eslint": "*",
        "@kitchensink/prettier": "*",
        "@kitchensink/typescript": "*",
        "@types/node": "^24.0.0",
        "aws-cdk": "*",
        "dotenv": "*",
        "esbuild": "^0.28.1",
        "eslint": "^10.0.3",
        "prettier": "^3.0.0",
        "tsx": "*",
        "typescript": "^5.9.3",
        "vitest": "^4.0.0"
    },
    "dependencies": {
        "aws-cdk-lib": "*",
        "constructs": "*"
    },
    "engines": {
        "node": "24.x"
    }
}
