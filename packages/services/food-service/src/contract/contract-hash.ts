/*
 * ⚠️ GENERATED FILE — DO NOT EDIT.
 *
 * Copied verbatim from the food (ingredient) service, which AUTHORS the wire contract. Edit the
 * source and regenerate: `npm run contract:generate --workspace=@kitchensink/food-service`.
 *
 * CI fails on any difference between this directory and a fresh regeneration, so a hand-edit here is
 * discarded rather than shipped.
 */

/**
 * Fingerprint of the food (ingredient) service's authored wire contract (service-embedded copy).
 *
 * SHA-256 over the authored `*.schema.ts` sources AND the composed sources they transitively
 * reach (see @kitchensink/contract-gen composed-sources.ts). The generator writes this value into
 * BOTH the service and the schema package, so a consumer pinned to an older schema package can
 * detect that the service it is talking to has moved ahead of it.
 */
export const CONTRACT_HASH = '4e7b73f12753ef494b9475f450796c14ca1d7df63d9bd21ce3951c541fb6de91';
