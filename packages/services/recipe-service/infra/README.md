# infra/

Workspace-local AWS CDK deployment code lives here. Add the per-type overlay
(see the bootstrap skill SKILL.md §C for the required stack for this workspace type).
Do NOT place CDK code outside this directory.
