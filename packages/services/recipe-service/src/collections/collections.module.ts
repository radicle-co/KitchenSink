import { Module } from '@nestjs/common';

/**
 * Collections module skeleton. Owns user recipe collections and their membership. Controllers,
 * services, and DAOs are added in a later phase.
 */
@Module({})
export class CollectionsModule {}
