import { Module } from '@nestjs/common';

/**
 * Common module skeleton for cross-cutting concerns (guards, interceptors, filters, shared
 * utilities). Providers are added in a later phase.
 */
@Module({})
export class CommonModule {}
