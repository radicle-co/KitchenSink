import { Module } from '@nestjs/common';

/**
 * Photos module skeleton. Owns recipe photo upload orchestration (presigned S3 uploads and the
 * `recipe_photos` completion update). Controllers, services, and DAOs are added in a later phase.
 */
@Module({})
export class PhotosModule {}
