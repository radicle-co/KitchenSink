import { Module } from '@nestjs/common';

/**
 * Search module skeleton. Owns recipe search and filtering. Controllers, services, and DAOs are
 * added in a later phase.
 */
@Module({})
export class SearchModule {}
