import { Module } from '@nestjs/common';

/**
 * Versions module skeleton. Owns recipe version history and archive retrieval. Controllers,
 * services, and DAOs are added in a later phase.
 */
@Module({})
export class VersionsModule {}
