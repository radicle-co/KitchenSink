---
title: Components — not generated yet
sidebar_label: Not generated yet
---

# Component documentation has not been generated in this checkout

This section is **generated**, not written. Nothing has been written to
`docs/generated/components/` in the commit you are looking at.

No example props, no sample renderings and no partial API tables are shown here, because inventing
any of them would be worse than showing nothing: a reader cannot tell a fabricated example from a
real one.

## What belongs here

The API documentation for the shared React component library — props, types and usage — derived from
the component sources themselves rather than transcribed by hand.

## How to fill it in

Run the component documentation generator, which writes into `docs/generated/components/` and commits
its output. The moment that directory contains Markdown, this page disappears and the real content is
mounted at the same route — the site needs no configuration change.
