/**
 * `@radicle-co/infra-shared/database` — where a stack that CREATES a database tells the rest of the platform
 * what it called it.
 *
 * @module
 */
export { recipeDatabaseNameParameter } from './recipeDatabaseParameter.js';
