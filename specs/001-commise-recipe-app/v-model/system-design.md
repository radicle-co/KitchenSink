# System Design: Commise - Recipe Management Core

**Feature Branch**: `001-commise-recipe-app`
**Created**: 2026-04-30
**Status**: Draft
**Source**: `specs/001-commise-recipe-app/v-model/requirements.md`

## Overview

This design decomposes the Commise Recipe Management Core into bounded system components aligned to the implemented architecture: NestJS API modules, web/mobile clients, PostgreSQL persistence, S3/CloudFront media storage, and SQS-backed asynchronous version archival. Decomposition follows requirement ownership boundaries (recipe commands, visibility policy, ingredients, search, photos, versioning, collections, GDPR erasure, the post-login Home widget surface, cross-cutting NFR governance) while preserving many-to-many traceability from every REQ-NNN to one or more SYS-NNN components.

## ID Schema

- **System Component**: `SYS-NNN` — sequential identifier for each component
- **Parent Requirements**: Comma-separated `REQ-NNN` list per component (many-to-many)
- Example: `SYS-003` with Parent Requirements `REQ-001, REQ-005` — component satisfies both requirements

## Decomposition View (IEEE 1016 §5.1)

| SYS ID  | Name                                           | Description                                                                                                                                          | Parent Requirements                                                                                                                                                                                                                   | Type      |
| ------- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| SYS-001 | Clerk Identity and Access Guard                | Verifies authenticated identity and enforces endpoint/UI access plus owner-access checks.                                                            | REQ-017, REQ-024, REQ-050b, REQ-052, REQ-IF-005a, REQ-IF-005b, REQ-IF-005c                                                                                                                                                            | Service   |
| SYS-002 | Recipe Command Service                         | Handles recipe create/update/delete/clone command flows, metadata persistence orchestration, and command-side validation wiring.                     | REQ-001, REQ-002, REQ-004, REQ-005, REQ-006, REQ-007, REQ-010a, REQ-010b, REQ-021, REQ-025, REQ-026a, REQ-026b                                                                                                                        | Subsystem |
| SYS-003 | Visibility and Source Policy Engine            | Applies subscription-tier and source-type visibility rules, substantive-edit unlock logic, and collection visibility policy constraints.             | REQ-022, REQ-023, REQ-027a, REQ-027b, REQ-028, REQ-050a, REQ-CN-001a, REQ-CN-001b, REQ-CN-003, REQ-CN-004a, REQ-CN-004b, REQ-CN-005                                                                                                   | Module    |
| SYS-004 | Ingredient Catalog and Nutrition Resolver      | Backs linked ingredients with nutrition data from the source-agnostic food service (003) via its typed client (`@kitchensink/food-service-client`) — foods referenced by internal id (`foodId`, never `fdcId`), resolution asynchronous — supports freeform (user-entered) ingredients, and powers ingredient suggestion/autocomplete. | REQ-003a, REQ-003b, REQ-031, REQ-032, REQ-033, REQ-057                                                                                                                                                                                | Service   |
| SYS-005 | Recipe Search and Filter Service               | Provides keyword search and structured filtering over active recipes with latency/SLO-aware query plans.                                             | REQ-018a, REQ-018b, REQ-029, REQ-030a, REQ-030b, REQ-030c, REQ-030d, REQ-030e, REQ-030f, REQ-NF-001, REQ-NF-002                                                                                                                       | Service   |
| SYS-006 | Photo Upload Validation and Attachment Service | Issues presigned uploads, performs server-side magic-byte revalidation, manages per-file errors/retries, and prevents broken references.             | REQ-008, REQ-009, REQ-010a, REQ-010b, REQ-013, REQ-014, REQ-015, REQ-016                                                                                                                                                              | Service   |
| SYS-007 | Photo Processing Lambda                        | Resizes uploaded originals into derivative renditions written to **S3 only** (S3-only, **NOT VPC-attached**, touches no DB per ADR-0004) and emits an SQS `photo-processed` message; the in-VPC Fargate API (SYS-006) consumes it and performs the `recipe_photos` completion write. | REQ-008, REQ-014, REQ-016                                                                                                                                                                                                             | Service   |
| SYS-008 | Recipe Versioning and Conflict Service         | Creates versions on save, enforces optimistic concurrency, and emits explicit conflict payloads without silent merge.                                | REQ-035, REQ-035a, REQ-035b, REQ-042a, REQ-042b, REQ-043, REQ-046                                                                                                                                                                     | Subsystem |
| SYS-009 | Version Archive Queue Producer                 | Queues asynchronous archive work after save and persists replayable pending-archive payload metadata.                                                | REQ-036, REQ-037, REQ-038, REQ-038a, REQ-038b, REQ-038c, REQ-039                                                                                                                                                                      | Service   |
| SYS-010 | Version Archive Worker and Replay Engine       | Consumes archive jobs, writes S3 version artifacts, retries failures, and clears pending rows only after confirmed success.                          | REQ-036, REQ-038, REQ-038a, REQ-038b, REQ-038c, REQ-039, REQ-040, REQ-NF-015                                                                                                                                                          | Service   |
| SYS-011 | Collection Management Service                  | Supports collection CRUD, membership management, visibility handling, and non-cascade delete semantics.                                              | REQ-047, REQ-048, REQ-049, REQ-049a, REQ-049b, REQ-050a, REQ-050b, REQ-056, REQ-CN-001a, REQ-CN-001b                                                                                                                                  | Subsystem |
| SYS-012 | Collection Clone and Pull Reconcile Service    | Implements collection snapshot cloning, source linkage, and user-initiated pull reconciliation rules.                                                | REQ-051, REQ-052, REQ-053, REQ-054, REQ-055a, REQ-055b, REQ-055c                                                                                                                                                                      | Service   |
| SYS-013 | GDPR Erasure Orchestrator                      | Enforces tombstone-retain-by-default and explicit account-erasure hard purge with idempotent erasure job semantics.                                  | REQ-019a, REQ-019b, REQ-019c, REQ-020a, REQ-020b, REQ-020c, REQ-IF-001a, REQ-IF-001b, REQ-IF-002, REQ-IF-003                                                                                                                          | Service   |
| SYS-014 | Data Access and Persistence Layer              | Encapsulates Drizzle/PostgreSQL repositories, tombstone-aware read filtering, relational integrity, and deterministic seeded test data support.      | REQ-003a, REQ-003b, REQ-016, REQ-018a, REQ-018b, REQ-019a, REQ-019b, REQ-019c, REQ-035, REQ-039, REQ-040, REQ-054, REQ-056, REQ-NF-002, REQ-NF-013a, REQ-NF-013b, REQ-NF-013c                                                         | Subsystem |
| SYS-015 | Object Storage and CDN Subsystem               | Stores photo objects and version archives in S3 and serves media through CloudFront.                                                                 | REQ-008, REQ-009, REQ-019b, REQ-020b, REQ-036, REQ-038, REQ-040                                                                                                                                                                       | Subsystem |
| SYS-016 | Web Client Application (Next.js)               | Delivers web UX for recipe/collection flows, client-side photo validation, conflict UI, accessibility, and web API endpoint parity.                  | REQ-011, REQ-012, REQ-014, REQ-015, REQ-034, REQ-044, REQ-045, REQ-IF-004a, REQ-IF-004c, REQ-IF-005b, REQ-IF-005c, REQ-NF-003, REQ-NF-007, REQ-NF-008, REQ-NF-018                                                                     | Subsystem |
| SYS-017 | Mobile Client Application (Expo/React Native)  | Delivers mobile UX parity, client-side validations, conflict UI, accessibility, and mobile API configuration behavior.                               | REQ-011, REQ-012, REQ-014, REQ-015, REQ-034, REQ-044, REQ-045, REQ-IF-004b, REQ-IF-004c, REQ-IF-005b, REQ-IF-005c, REQ-NF-003, REQ-NF-007, REQ-NF-008, REQ-NF-019                                                                     | Subsystem |
| SYS-018 | Configuration and Environment Resolution       | Centralizes typed environment loading, defaults, and local port/base-URL resolution across API, web, and mobile.                                     | REQ-NF-018, REQ-NF-019, REQ-NF-020                                                                                                                                                                                                    | Utility   |
| SYS-019 | Observability and Alerting                     | Emits archive-failure operational signals and enforces backlog health alarms/telemetry needed for operations and engagement analysis.                | REQ-041, REQ-NF-004, REQ-NF-015, REQ-NF-016, REQ-NF-017                                                                                                                                                                               | Utility   |
| SYS-020 | Quality, Test, and CI Governance               | Enforces strict TypeScript/JSDoc rules, TDD/testing conventions, LocalStack-based test execution, CI gates, and out-of-scope constraint enforcement. | REQ-CN-002, REQ-NF-005a, REQ-NF-005b, REQ-NF-006, REQ-NF-009, REQ-NF-010a, REQ-NF-010b, REQ-NF-011a, REQ-NF-011b, REQ-NF-011c, REQ-NF-012a, REQ-NF-012b, REQ-NF-013a, REQ-NF-013b, REQ-NF-013c, REQ-NF-014a, REQ-NF-014b, REQ-NF-014c | Utility   |
| SYS-021 | Home Widget Surface                            | Post-login Home screen as a cross-platform **widget surface** (web + mobile). Discovers widgets by explicit startup registration (composition root, `.use(addFeature)`), composes them with the pure `curateHomeWidgets(widgets, ctx)` — gating by **capability** (is the backing service live) and **subscription tier**, ordering by per-user **personalization** — and renders via `React.lazy` + Suspense + per-widget ErrorBoundary, skipping unknown ids. In v1 the recent-recipes (recipe) widget is the **only registered** widget; the meal-plan, nutrition, shopping-list, AI-suggestion, and resume-cooking widgets (backed by services **005–009**) are **not registered in v1** — their feature packages do not exist yet and a literal `import()` loader for an unbuilt package would fail the build — so they are **absent** from Home. Each is registered (with its loader) when its feature package ships; **capability gating then shows it once its backing service is live.** A widget whose backing service is absent MUST NOT render (absent, not a dead/empty tile); per-widget empty states apply only to live widgets. Emits a once-per-session premium nudge. Per-user layout (order, hidden) is persisted via `PATCH /v1/profiles/me`, **owned by the identity service (002)** and consumed here. Design authority: `research/home-widget-architecture.md` (`## DECISION (2026-07-06)`). | REQ-058, REQ-058a, REQ-058b, REQ-058c, REQ-058d, REQ-058e, REQ-058f, REQ-058g, REQ-058h, REQ-058i, REQ-IF-006 | Subsystem |

## Dependency View (IEEE 1016 §5.2)

| Source  | Target  | Relationship          | Failure Impact                                                                                                                                                               |
| ------- | ------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SYS-016 | SYS-001 | Calls                 | Web users cannot authenticate/authorize, blocking all protected actions.                                                                                                     |
| SYS-017 | SYS-001 | Calls                 | Mobile users cannot authenticate/authorize, blocking all protected actions.                                                                                                  |
| SYS-016 | SYS-005 | Calls                 | Recipe search/filter unavailable in web UX.                                                                                                                                  |
| SYS-017 | SYS-005 | Calls                 | Recipe search/filter unavailable in mobile UX.                                                                                                                               |
| SYS-016 | SYS-002 | Calls                 | Recipe create/update/delete/clone commands are unavailable in web UX.                                                                                                        |
| SYS-017 | SYS-002 | Calls                 | Recipe create/update/delete/clone commands are unavailable in mobile UX.                                                                                                     |
| SYS-002 | SYS-003 | Uses                  | If SYS-003 is unavailable, SYS-002 cannot validate visibility/substantive-edit rules, causing recipe mutations to be accepted or rejected without proper policy enforcement. |
| SYS-002 | SYS-004 | Calls                 | Ingredient linkage and nutrition enrichment fail during recipe save.                                                                                                         |
| SYS-002 | SYS-006 | Calls                 | Photo attach/confirm flows fail and per-file retry UX degrades.                                                                                                              |
| SYS-002 | SYS-008 | Calls                 | Saves cannot produce required versions or concurrency guards.                                                                                                                |
| SYS-002 | SYS-014 | Writes                | Recipe metadata cannot persist atomically.                                                                                                                                   |
| SYS-005 | SYS-001 | Calls                 | Search endpoint cannot enforce authentication, exposing recipes to unauthenticated callers.                                                                                  |
| SYS-005 | SYS-014 | Reads                 | Search/filter requests time out or return empty/incorrect results.                                                                                                           |
| SYS-006 | SYS-015 | Writes                | Presigned upload and object verification fail, blocking photo ingestion.                                                                                                     |
| SYS-006 | SYS-007 | Triggers via S3 Event | Photo processing is not initiated; photos remain in non-complete state indefinitely.                                                                                         |
| SYS-007 | SYS-015 | Reads/Writes          | Photo derivatives are not generated, leaving photos in non-complete state.                                                                                                   |
| SYS-007 | SYS-006 | Emits via SQS (photo-processed) | Completion message is never emitted; the Fargate consumer cannot run the `recipe_photos` completion write and photos remain in `PHOTO_PROCESSING` indefinitely.               |
| SYS-006 | SYS-014 | Writes                | `recipe_photos` rows and the `photo-processed` completion `UPDATE` (S3 keys, `processing_status`) cannot persist; photos never reach a complete state.                        |
| SYS-008 | SYS-009 | Calls                 | Archive work is never enqueued, causing unarchived version growth.                                                                                                           |
| SYS-010 | SYS-009 | Subscribes            | Pending archive backlog accumulates because queue work is not drained.                                                                                                       |
| SYS-010 | SYS-014 | Writes                | Pending rows and archive state cannot transition correctly.                                                                                                                  |
| SYS-010 | SYS-015 | Writes                | Version snapshots are not archived to S3.                                                                                                                                    |
| SYS-011 | SYS-014 | Reads/Writes          | Collection CRUD and membership operations cannot persist; all collection mutations fail.                                                                                     |
| SYS-011 | SYS-001 | Calls                 | Collection ownership and visibility checks cannot be authorized, blocking all collection mutations by non-owners or free-tier users.                                         |
| SYS-012 | SYS-014 | Writes                | Cloned collection snapshots and pull reconciliation results cannot persist; clone/pull operations fail without side effects.                                                 |
| SYS-011 | SYS-012 | Calls                 | Clone/pull semantics diverge from snapshot and reconciliation rules.                                                                                                         |
| SYS-013 | SYS-014 | Calls                 | GDPR erase cannot complete DB-side hard purge safely.                                                                                                                        |
| SYS-013 | SYS-015 | Calls                 | S3 versions/photos remain after erasure request completion.                                                                                                                  |
| SYS-013 | SYS-001 | Calls                 | Erasure endpoint cannot verify caller identity, enabling unauthorized erasure requests.                                                                                      |
| SYS-019 | SYS-010 | Reads                 | Archive failures and backlog breaches go undetected operationally.                                                                                                           |
| SYS-020 | SYS-016 | Uses                  | Web quality/accessibility/test conformance drifts from required standards.                                                                                                   |
| SYS-020 | SYS-017 | Uses                  | Mobile quality/accessibility/test conformance drifts from required standards.                                                                                                |
| SYS-016 | SYS-021 | Hosts                 | Web Home screen cannot render the widget surface; users land without a curated post-login entry point.                                                                       |
| SYS-017 | SYS-021 | Hosts                 | Mobile Home screen cannot render the widget surface; users land without a curated post-login entry point.                                                                    |
| SYS-021 | SYS-001 | Calls                 | Home widget surface cannot enforce authentication; the post-login screen is exposed without a verified identity.                                                             |
| SYS-021 | SYS-002 | Calls                 | The live recent-recipes widget cannot load recipe data; the only v1 widget renders empty/errored.                                                                            |

### Dependency Diagram

```text
Users
 ├─> SYS-016 Web Client ─┬─> SYS-001 Auth Guard
 │                       ├─> SYS-002 Recipe Commands ─┬─> SYS-003 Policy
 │                       │                           ├─> SYS-004 Ingredients
 │                       │                           ├─> SYS-006 Photo Upload ─┬─> SYS-015 S3/CloudFront
 │                       │                           │                         └─(S3 Event)─> SYS-007 Photo Processor (S3-only) ─> SYS-015 S3/CloudFront
 │                       │                           ├─> SYS-008 Versioning ─> SYS-009 Archive Producer
 │                       │                           └─> SYS-014 Persistence
 │                       ├─> SYS-005 Search ─┬─> SYS-001 Auth Guard
 │                       │                   └─> SYS-014 Persistence
 │                       ├─> SYS-011 Collections ─┬─> SYS-001 Auth Guard
 │                       │                        ├─> SYS-012 Clone/Pull Reconcile ─> SYS-014 Persistence
 │                       │                        └─> SYS-014 Persistence
 │                       └─> SYS-013 GDPR Erasure ─┬─> SYS-001 Auth Guard
 │                                                 ├─> SYS-014 Persistence
 │                                                 └─> SYS-015 S3/CloudFront
 └─> SYS-017 Mobile Client ─┬─> SYS-001 Auth Guard
                             ├─> SYS-002 Recipe Commands
                             ├─> SYS-005 Search
                             └─> SYS-013 GDPR Erasure

SYS-010 Archive Worker <─(SQS)─ SYS-009 Archive Producer
SYS-010 Archive Worker ─┬─> SYS-014 Persistence
                         └─> SYS-015 S3/CloudFront

SYS-007 Photo Processor ─(SQS photo-processed)─> SYS-006 Photo Upload/Attach (Fargate consumer) ─> SYS-014 Persistence (recipe_photos completion)

SYS-016 Web Client / SYS-017 Mobile Client ─> SYS-021 Home Widget Surface ─┬─> SYS-001 Auth Guard
                                                                            ├─> SYS-002 Recipe Commands (recent-recipes widget, only live widget in v1)
                                                                            └─(consumes)─> PATCH /v1/profiles/me (identity service 002)

SYS-019 Observability monitors SYS-010
SYS-020 Quality/CI governs SYS-016 and SYS-017
```

## Interface View (IEEE 1016 §5.3)

### External Interfaces

| Component | Interface Name                                                                                                                   | Protocol                           | Input                                                                                                           | Output                                                                      | Error Handling                                                                                                                                                                                                                                                                                                                                                        |
| --------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SYS-001   | Clerk Networkless Session-Token Verification                                                                                     | In-process (JWT verify)            | Bearer Clerk session token, configured `CLERK_JWT_KEY` public key, `CLERK_AUTHORIZED_PARTIES` (`azp`) allowlist | Verified Principal — `userId` (**app-user ULID**, read from the token's `external_id` claim; **THE owner key**), `sub` (Clerk subject, retained for trace/audit only, **never** an owner key), `email`, `tier`, roles/permissions (from token `public_metadata`). Ownership everywhere compares `owner_id == principal.userId` (ULID == ULID). **`@commise/shared-clerk-verify` (`VerifiedClerkClaims`/`ClerkAuthService`) MUST be extended to surface the `external_id` claim as `userId` — a shipped-code change, not a given; today the verified claims expose only `sub`/`azp`/`email`/names/`scopes`/`permissions` (no `external_id`/`userId`).** | Reject invalid/expired tokens or disallowed `azp` as 401; deny unauthorized scopes as 403. A token whose `external_id` (app-user ULID) claim is absent/empty **fails verification** — the owner key is a prerequisite, not optional (see REQ-IF-007), and `sub` is never used as a fallback owner key.                                                                                                                                                                                                                                                                            |
| SYS-002   | Recipes Command API (`/v1/recipes`, `/v1/recipes/{id}`, `/v1/recipes/{id}/clone`, `/v1/recipes/{id}/visibility`) | HTTPS REST/JSON                    | `CreateRecipeRequest`, `UpdateRecipeRequest`, visibility payload, path/query params                             | `Recipe` or `204` for delete                                                | Returns structured 400/401/403/404/409 with validation and conflict details; no silent mutation on failure. Visibility/substantive-edit policy denials from SYS-003 surface as 403 with `ruleId` (e.g., `PREMIUM_ONLY_PRIVATE`, `PAID_SOURCE_NEVER_PUBLIC`, `SUBSTANTIVE_EDIT_REQUIRED`); policy service unavailability surfaces as 503 `POLICY_SERVICE_UNAVAILABLE`. |
| SYS-004   | Ingredients API (`/v1/ingredients/search`, `/v1/ingredients`)                                                                    | HTTPS REST/JSON                    | Query `q`, limit, `CreateIngredientRequest`                                                                     | `Ingredient[]` / `Ingredient`                                               | 400 for invalid input, 401 for auth failure; deterministic error body. Ingredient data is backed by the source-agnostic food service (003) via its typed client (`@kitchensink/food-service-client` → `foodClient.search(query)`), with foods referenced by the food service's internal id (`foodId`, never the source `fdcId`) and nutrition resolving **asynchronously** — a just-added food may return `PENDING`/`UNRESOLVED` with `nutritionResolved: false` until resolution completes. If the food service is unavailable, ingredient resolution falls back to the freeform (user-entered) path per REQ-032 and returns a partial result with `nutritionResolved: false` rather than 503. |
| SYS-005   | Search API (`/v1/search/recipes`)                                                                                                | HTTPS REST/JSON                    | Free-text query + filters (tags, cuisine, dietary, ingredient, time)                                            | `RecipeSearchResponse` with facets/pagination                               | 400 for invalid filters, 401 for auth failure. Queries exceeding the 2.0-second SLO return HTTP 504 with `SEARCH_TIMEOUT`; SYS-014 unavailability surfaces as HTTP 503 with `PERSISTENCE_UNAVAILABLE`.                                                                                                                                                                |
| SYS-006   | Photo Upload API (`/v1/recipes/{id}/photos/upload-url`, `/v1/recipes/{id}/photos/confirm`)                                       | HTTPS REST/JSON + S3 Presigned PUT | `PhotoUploadUrlRequest`, `PhotoConfirmRequest`, binary upload via signed URL                                    | `UploadUrlResponse`, `RecipePhoto`                                          | Per-file validation failures return specific reason; failed uploads are retryable without recipe re-save. Successful `confirm` transitions photo lifecycle `PHOTO_PENDING → PHOTO_PROCESSING`.                                                                                                                                                                        |
| SYS-011   | Collections API (`/v1/collections`, `/v1/collections/{id}`, `/v1/collections/{id}/recipes`)                                      | HTTPS REST/JSON                    | Create/update payloads, membership add/remove payloads                                                          | `Collection`, `CollectionWithRecipes`, membership records                   | 401/403/404 for auth/ownership/resource checks; 400 for invalid state transitions. Collection creation beyond per-user cap (value TBD per REQ-049b) returns HTTP 422 with `COLLECTION_CAP_EXCEEDED` (known open item until REQ-049b cap value is resolved).                                                                                                           |
| SYS-012   | Collection Clone/Pull API (`/v1/collections/{id}/clone`, `/v1/collections/{id}/pull-from-source`)                                | HTTPS REST/JSON                    | Collection path id, optional clone overrides, pull invocation                                                   | Cloned `Collection` / `PullFromSourceResponse`                              | `COLLECTION_NOT_CLONED` and access violations returned as 400/403; operations remain user-initiated only.                                                                                                                                                                                                                                                             |
| SYS-013   | Account Erasure API (`POST /v1/account/erasure`)                                                                                 | HTTPS REST/JSON                    | Optional `ErasureRequest` confirmation phrase                                                                   | HTTP 202 with job id for queued/running/fresh retry, HTTP 410 for completed | Idempotency contract: no duplicate queued/running jobs; terminal completed returns 410; failed state creates new job id. `202` corresponds to `ERASURE_REQUESTED`; `410` corresponds to `ERASURE_COMPLETED`.                                                                                                                                                          |
| SYS-015   | Media Delivery Interface (S3 + CloudFront)                                                                                       | HTTPS (S3 object API, CDN GET)     | Signed PUT uploads and CDN GET requests for photo assets                                                        | Object write acknowledgment, cached media responses                         | Expired signatures/content-length violations rejected; storage errors surfaced to API/worker logs and retries.                                                                                                                                                                                                                                                        |
| SYS-021   | Home Layout Persistence (`PATCH /v1/profiles/me` — **owned by identity service 002**, consumed here)                    | HTTPS REST/JSON                    | Per-user layout diff (`order`, `hidden`) as a sparse patch                                                     | Persisted layout acknowledgment                                             | External dependency: 002 owns the per-user DB and this endpoint (`REQ-IF-006`). 001 consumes it; when unavailable, Home falls back to the default widget order (no reorder/hide persistence) rather than failing to render. Unknown widget ids in a stored layout are skipped on curate.                                                                              |

### Internal Interfaces

| Source  | Target  | Interface Name                           | Protocol                          | Data Format                                                                            | Error Handling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------- | ------- | ---------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SYS-002 | SYS-014 | RecipeRepository Contract                | In-process DI + SQL               | Typed DTOs/Drizzle models                                                              | Transaction rollback on DB failure; domain errors mapped to API error codes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SYS-002 | SYS-003 | VisibilityPolicy Evaluation              | In-process function call          | Recipe context + tier/source metadata                                                  | Policy evaluations return a structured `{ allowed: boolean, reason: string, ruleId: string }` result. Denials include a `ruleId` identifying the specific policy rule (e.g., `PREMIUM_ONLY_PRIVATE`, `PAID_SOURCE_NEVER_PUBLIC`, `SUBSTANTIVE_EDIT_REQUIRED`). If SYS-003 is unavailable or throws, SYS-002 defaults to deny (fail-closed) and surfaces a `POLICY_SERVICE_UNAVAILABLE` error to the API layer for translation to 503.                                                                                                                                                                               |
| SYS-002 | SYS-004 | Ingredient Resolution Contract           | In-process call                   | Ingredient ids/text + quantity/unit                                                    | Unresolved links fall back to user-entered path with explicit flags.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| SYS-002 | SYS-008 | Versioning + Conflict Contract           | In-process service call           | Recipe save payload + client version/ETag                                              | Success returns new version record metadata; stale version returns deterministic 409 conflict payload (no silent merge).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| SYS-006 | SYS-015 | Presigned Upload and Confirm Storage API | AWS SDK over HTTPS                | Object key, content-type, size, magic-byte metadata                                    | Signature/validation failures returned as per-file errors with retryability metadata.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| SYS-006 | SYS-007 | S3 Object Created Event                  | S3 Event Notification/EventBridge | Event JSON (`bucket`, `key`, `metadata`)                                               | Invocation consumes the staged object event. The Lambda is **S3-only and touches no DB** (ADR-0004): it resizes to S3 renditions and then emits an SQS `photo-processed` message. The `PHOTO_PROCESSING → NORMAL`/`PHOTO_FAILED` lifecycle transition is applied by the Fargate consumer (SYS-007→SYS-006 below), **not** by the Lambda. Failed processing retried by Lambda/S3 event semantics; photo remains non-complete until the completion write succeeds.
| SYS-007 | SYS-006 | Photo-Processed Completion Message       | AWS SDK SQS `SendMessage` → poll  | Message JSON `{ recipePhotoId, s3_key_thumb, s3_key_card, s3_key_full, status }`       | The in-VPC Fargate API (SYS-006) polls the `photo-processed` queue and performs the sole DB write for photo completion: `UPDATE recipe_photos SET s3_key_* = …, processing_status = 'complete'` (or `'failed'`) `WHERE id = recipePhotoId`, transitioning the photo lifecycle `PHOTO_PROCESSING → NORMAL` (or `→ PHOTO_FAILED`). Retries/DLQ on repeated consumer failure; the Lambda never writes the DB.                                                                                                                                                                                                                                                                                              |
| SYS-008 | SYS-009 | Archive Enqueue Interface                | AWS SDK SQS `SendMessage`         | Message JSON with `recipeVersionId` and correlation ids                                | Enqueue failures do not fail user save; payload remains in pending table for sweeper replay.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SYS-010 | SYS-009 | Archive Work Consumption                 | SQS poll/invoke                   | Archive job message payload                                                            | Automatic retries and DLQ redrive on repeated failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| SYS-010 | SYS-015 | Version Snapshot Archive Write           | AWS SDK S3 `PutObject`            | Version snapshot JSONB serialized payload                                              | On failure: increment attempts, retain pending row, retry; on success: continue state transition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| SYS-010 | SYS-014 | Pending Archive State Transition         | SQL update/delete                 | Pending-archive row, `s3_key`, attempt counters                                        | Pending row deletion only after confirmed S3 success; idempotent reprocessing supported.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| SYS-011 | SYS-012 | Collection Sync Orchestration            | In-process service call           | Source/clone collection ids and membership provenance (`manual`, `clone_seed`, `pull`) | Returns `{ cloned: Collection, skipped: SkippedItem[] }` on success, where `SkippedItem = { recipeId: string, reason: 'NOT_ACCESSIBLE' \| 'NOT_PUBLIC' \| 'SOURCE_DELETED' }`. On source-not-found: throws `SOURCE_COLLECTION_NOT_FOUND` (mapped to API 404). On source-deleted-mid-clone: partial result returned with `SOURCE_DELETED_MID_OPERATION` warning; already-cloned items preserved. On partial-reconciliation: returns `{ appliedAdds: number, appliedRemovals: number, preservedManual: number, skippedItems: SkippedItem[] }`. Fail-closed on unexpected errors — no partial state mutation on throw. |
| SYS-013 | SYS-015 | Erasure Storage Purge Interface          | AWS SDK S3 list/delete            | User-owned recipe prefixes, object keys                                                | Partial purge errors retried; completion only after storage deletion success.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| SYS-019 | SYS-010 | Archive Health Telemetry Feed            | CloudWatch Metrics/Logs           | Failure counts, pending-row depth, oldest-age metric                                   | Alarm routing on threshold breach; no end-user blocking due to monitoring path failures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

> **Interface scope note (IEEE 1016 §5.3 completeness):** SYS-018 (Configuration and Environment Resolution) and SYS-020 (Quality, Test, and CI Governance) are non-runtime utility/governance components with no direct runtime API surface. SYS-019 is operational telemetry/alerting only and does not expose a consumer-facing API beyond internal telemetry feed contracts documented above.

## Data Design View (IEEE 1016 §5.4)

| Entity                                                           | Component         | Storage                                             | Protection at Rest                                                               | Protection in Transit                                   | Retention                                                                                                                                                       |
| ---------------------------------------------------------------- | ----------------- | --------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `recipes`                                                        | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption + row-level ownership enforcement in service layer (`owner_id` = app-user ULID (from the token claim), `VARCHAR NOT NULL`, **no FK / no local `users` table**) | TLS 1.2+                                                | Active rows retained indefinitely; tombstoned rows retained indefinitely by default until REQ-020a explicit erasure trigger, then purged per REQ-020b/REQ-020c. |
| `recipe_steps`                                                   | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption + FK integrity                                                    | TLS 1.2+                                                | Retained with recipe lifecycle; removed only via explicit hard purge.                                                                                           |
| `ingredients`                                                    | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption + controlled write paths for user-entered/manual nutrition fields; foods referenced by opaque `foodId` (food service internal id, never `fdcId`) with a 001-owned resolution-status field, no cross-DB FK to the food service | TLS 1.2+                                                | Retained for lookup/history; user-entered records follow owning recipe/account retention rules.                                                                 |
| `recipe_ingredients`                                             | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption + FK constraints to recipes/ingredients                           | TLS 1.2+                                                | Retained with recipe history; deleted only through explicit hard purge path.                                                                                    |
| `recipe_tags`                                                    | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption at rest (AWS-managed KMS)                                         | TLS 1.2+ from API services to RDS                       | Retained with recipe lifecycle; removed only via explicit hard purge. SYS-002 owns write paths; SYS-005 consumes via read-only queries.                         |
| `recipe_photos`                                                  | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption + key metadata access controlled to owner/authenticated readers   | TLS 1.2+                                                | Retained while recipe exists/tombstone retained; removed on explicit photo delete or account erasure.                                                           |
| `recipe_versions`                                                | SYS-014           | PostgreSQL table (RDS, JSONB snapshots)             | RDS encryption + append/version ownership checks                                 | TLS 1.2+                                                | Keep latest 10 versions in DB per recipe; older history preserved in S3 archives.                                                                               |
| `recipe_version_pending_archives`                                | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption + operational-role restricted access                              | TLS 1.2+                                                | Retained until corresponding S3 archive success is confirmed; no TTL auto-purge.                                                                                |
| `collections`                                                    | SYS-014           | PostgreSQL table (RDS)                              | RDS encryption + owner authorization checks                                      | TLS 1.2+                                                | Retained until owner delete or account erasure; cloned `sourceCollectionId` preserved while record exists.                                                      |
| `recipe_collections`                                             | SYS-014           | PostgreSQL join table (RDS)                         | RDS encryption + provenance (`added_via`) controls                               | TLS 1.2+                                                | Retained while membership exists; independent from recipe/collection delete semantics per non-cascade rules.                                                    |
| Photo objects (`uploads/...`, `photos/...`)                      | SYS-015           | Amazon S3 objects + CloudFront cache                | SSE-S3 encryption at rest, bucket policies, OAC/private origins                  | HTTPS/TLS for PUT/GET via presigned URLs and CloudFront | Retained while referenced by active/tombstoned recipes; deleted by explicit photo delete or account erasure purge.                                              |
| Version archive objects (`versions/{recipe_id}/v{n}.json`)       | SYS-015           | Amazon S3 objects                                   | SSE-S3 encryption + bucket IAM boundaries                                        | HTTPS/TLS via AWS SDK                                   | Retained indefinitely by default; removed only by explicit GDPR erasure action.                                                                                 |
| Version-archive queue and DLQ messages                           | SYS-009 / SYS-010 | Amazon SQS standard queue + DLQ                     | SSE-SQS encryption + IAM queue policies                                          | HTTPS/TLS between producers/consumers and SQS           | Message retained until successful processing/ack; failed messages redriven to DLQ for operator replay window.                                                   |
| `CONFIG_STORE` (runtime environment variable tree)               | SYS-018           | In-process configuration object (Node.js)           | No persistence — sourced fresh per process startup; validated at startup         | Not applicable (in-process only)                        | Lifecycle tied to process restart; no persistent storage.                                                                                                       |
| CloudWatch Metrics and Logs (telemetry event stream)             | SYS-019           | AWS CloudWatch / embedded metrics payload object    | CloudWatch default encryption + IAM subscribe policies                           | HTTPS/TLS from services to CloudWatch API               | 90-day CloudWatch log retention (default); metrics retained 15 months.                                                                                          |
| `CI_PIPELINE_ARTIFACT` (JSON policy rule + test metadata store)  | SYS-020           | GitHub Actions environment context + CI config YAML | GitHub repo visibility + branch protection rules                                 | HTTPS via GitHub Actions runtime token                  | Retained as part of GitHub Actions run artifact (90-day GitHub retention); policy YAML persisted in version-controlled source.                                  |
| `account_erasure_jobs` (`queued`/`running`/`failed`/`completed`) | SYS-013           | PostgreSQL table (RDS, logical erasure job ledger)  | RDS encryption + idempotency state constraints                                   | TLS 1.2+                                                | Retained for idempotency and auditability of erasure invocations and terminal outcomes.                                                                         |

---

## Operational States

The system exposes the following operational states across its asynchronous lifecycles. These states are referenced by hazard analysis (`HAZ-NNN`) entries and bound to the components that own each state machine. `NORMAL` denotes steady-state synchronous request/response handling outside any of the lifecycles below; `ALL` is reserved for hazards whose severity is invariant across every state.

| STATE               | Owning Component(s)        | Description                                                                                                               | Entry / Exit                                                                                                                                                          |
| ------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NORMAL              | All synchronous services   | Steady-state online operation handling synchronous API/web/mobile traffic outside any async lifecycle below.              | Default state. Exits transiently when a request enters a specific async lifecycle below.                                                                              |
| PHOTO_PENDING       | SYS-006 (Photo upload)     | Client has obtained a presigned URL and is uploading a photo object to S3 staging (`uploads/...`); no DB row yet.         | Enter: presigned URL issued. Exit: upload completes (→ `PHOTO_PROCESSING`) or aborts/expires (→ `PHOTO_FAILED`).                                                      |
| PHOTO_PROCESSING    | SYS-007 (Photo Lambda, resize) / SYS-006 (Fargate consumer, DB write) | Sharp Lambda normalizes/validates the staged photo and writes the canonical S3 renditions (**S3 only, no DB** per ADR-0004), then emits an SQS `photo-processed` message; the in-VPC Fargate API (SYS-006) consumes it and performs the `recipe_photos` completion `UPDATE`. | Enter: S3 event triggers Lambda. Exit: Fargate consumer applies the completion write — success (`status='complete'`) → `NORMAL`; failure (`status='failed'`) → `PHOTO_FAILED`.                                                                                  |
| PHOTO_FAILED        | SYS-006 (Photo upload)     | Photo upload or processing did not complete; staged object may be orphaned and the recipe has no canonical photo linked.  | Enter: upload abort, presigned URL expiry, validation failure, or a `photo-processed` message with `status='failed'`. Exit: operator/cleanup job purges staged object, or user retries (→ `PHOTO_PENDING`). |
| ARCHIVE_PENDING     | SYS-009 (Archive producer) | A new recipe version snapshot has been written to RDS and an archive job row exists but has not yet been enqueued/sent.   | Enter: version snapshot persisted. Exit: SQS enqueue acknowledged (→ `ARCHIVE_IN_FLIGHT`).                                                                            |
| ARCHIVE_IN_FLIGHT   | SYS-010 (Archive consumer) | Archive job has been delivered to the version-archive queue and the consumer is writing `versions/{recipe_id}/v{n}.json`. | Enter: SQS message in-flight to consumer. Exit: S3 PutObject acknowledged and pending row cleared (→ `NORMAL`); persistent failure routes to `ARCHIVE_DEAD_LETTER`.   |
| ARCHIVE_DEAD_LETTER | SYS-010 (Archive DLQ)      | Archive job exhausted retries and is parked on the DLQ awaiting operator triage/replay.                                   | Enter: max-receive exceeded on main queue. Exit: operator replays message back onto main queue (→ `ARCHIVE_IN_FLIGHT`) or marks the job permanently abandoned.        |
| ERASURE_REQUESTED   | SYS-013 (Erasure service)  | A GDPR erasure request has been accepted and a job ledger row created in `queued` state; no destructive work has begun.   | Enter: erasure API accepts the request. Exit: worker picks up the job (→ `ERASURE_IN_PROGRESS`).                                                                      |
| ERASURE_IN_PROGRESS | SYS-013 (Erasure worker)   | Erasure worker is purging owned RDS rows and S3 objects (photos + version archives) for the subject account.              | Enter: worker leases the job row. Exit: all targeted artifacts purged and ledger marked `completed` (→ `ERASURE_COMPLETED`); fatal failure leaves ledger in `failed`. |
| ERASURE_COMPLETED   | SYS-013 (Erasure ledger)   | Erasure job has reached a terminal `completed` state; only the audit/idempotency ledger row is retained for the subject.  | Enter: worker finishes purge successfully. Exit: terminal state. Re-requests are idempotent against this ledger row.                                                  |

---

## Coverage Summary

| Metric                            | Count                                                               |
| --------------------------------- | ------------------------------------------------------------------- |
| Total System Components (SYS)     | 21                                                                  |
| Total Parent Requirements Covered | 129 / 129 (100%)                                                    |
| Components per Type               | Subsystem: 8 \| Module: 1 \| Service: 9 \| Library: 0 \| Utility: 3 |
| **Forward Coverage (REQ→SYS)**    | **100%**                                                            |

> **Coverage metric definition:** The denominator uses **all requirement IDs from `requirements.md` (134 total)**, including atomic sub-IDs (e.g., `REQ-035a`, `REQ-038c`, `REQ-NF-011b`) but excluding non-normative umbrella index entries (REQ-035, REQ-038, REQ-049). This aligns Matrix B forward-coverage computation with `build-matrix.sh` expectations.

> **Coverage regeneration note:** The `134 / 134` figure is regenerated by `build-matrix.sh` (deterministic) and MUST NOT be hand-edited. After any change to `requirements.md` or to the Parent Requirements columns above, re-run `build-matrix.sh` to refresh this count; CI gates the artifact against drift.

> **Pending Home (FR-046 / US-0) coverage:** `SYS-021 Home Widget Surface` traces to the FR-046 Home-widget requirement decomposition (`REQ-058` + `REQ-058a…i`) and the 002-owned profile-preferences dependency (`REQ-IF-006`). These REQ IDs are added to `requirements.md` in the same reconciliation pass; once present, `build-matrix.sh` re-runs and updates the coverage numerator/denominator above (the `129 / 129` and `134 total` figures are pre-Home and will rise accordingly). Confirm the final REQ IDs against `requirements.md` before regeneration.

> **Note on low-granularity requirements:** Some fine-grained requirements (e.g., REQ-006 for servings count, REQ-007 for tags/categories, REQ-034 for user-entered ingredient notice) are satisfied by composite component boundaries within SYS-002 or SYS-004 rather than independent behavioral specifications. These fields are modeled as parts of the parent command or query flow and share the same interface and data design entries as their enclosing component. Coverage is complete, but the behavioral specification for these individual fields is folded into the broader component contract.

## Derived Requirements: None

There are no derived requirements in this design.

### Design Notes

All components trace to explicit requirements listed in the Decomposition View. SYS-018 (Configuration and Environment Resolution) and SYS-019 (Observability and Alerting) are cross-cutting infrastructure components whose parent requirements are non-functional (REQ-NF-018/019/020 and REQ-041/REQ-NF-004/015/016/017 respectively). While their traceability to REQs is complete, their implementation pattern differs from the direct feature-requirement-to-component mapping of SYS-001 through SYS-017: they enforce constraints and provide operational infrastructure rather than implementing user-facing capabilities.

**Configuration ownership split (REQ-NF-018 / REQ-NF-019 / REQ-NF-020):** SYS-018 is the sole **provider** of typed configuration loading, default resolution, and environment-variable validation across API, web, and mobile. SYS-016 (Web Client) and SYS-017 (Mobile Client) are **consumers** of resolved configuration values and own correct runtime behavior given those values (e.g., honoring resolved API base URLs, port mappings, and feature flags). Downstream Matrix B verification SHOULD test loader/default behavior against SYS-018 and consumption/runtime behavior against SYS-016 (REQ-NF-018) and SYS-017 (REQ-NF-019); REQ-NF-020 is verified solely against SYS-018.

## Peer-Review Remediation Log

| Finding ID  | Action Taken                                                                                                                                                                                                                                   |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PRF-SYS-001 | Updated Coverage Summary from `87 / 87` to `129 / 129` and added explicit metric-definition note that coverage counts all IDs (umbrella + atomic sub-IDs) in alignment with Matrix B computation expectations.                                 |
| PRF-SYS-002 | Confirmed/retained ownership mapping of REQ-NF-003 to SYS-016 and SYS-017 and REQ-NF-004 to SYS-019 in Decomposition View; forward coverage now reflected in updated 129-ID denominator.                                                       |
| PRF-SYS-003 | Added atomic sub-ID traces: `REQ-035a/REQ-035b` to SYS-008, `REQ-038a/REQ-038b/REQ-038c` to SYS-009 and SYS-010, and `REQ-049a/REQ-049b` to SYS-011 while retaining umbrella IDs for readability.                                              |
| PRF-SYS-004 | Added Dependency View rows: SYS-016→SYS-005, SYS-017→SYS-005, SYS-005→SYS-001, and SYS-013→SYS-001 with the specified failure impacts.                                                                                                         |
| PRF-SYS-005 | Added missing dependency row SYS-006→SYS-007 (Triggers via S3 Event); retained SYS-007→SYS-015 Reads/Writes dependency and aligned table/diagram representation.                                                                               |
| PRF-SYS-006 | Extended SYS-004 external-interface error handling to contractually specify degraded behavior when food DB is unavailable (`nutritionResolved: false` fallback path, no 503).                                                                  |
| PRF-SYS-007 | Replaced SYS-005 interface implementation note with explicit contractual timeout/unavailability behavior: HTTP 504 `SEARCH_TIMEOUT` and HTTP 503 `PERSISTENCE_UNAVAILABLE`.                                                                    |
| PRF-SYS-008 | Added explicit SYS-002→SYS-008 internal interface contract (versioning/conflict API), and added Interface View scope note explicitly excluding SYS-018 and SYS-020 from runtime API surface while clarifying SYS-019’s telemetry-only surface. |
| PRF-SYS-009 | Renamed section to `Derived Requirements: None`, added explicit statement that no derived requirements exist, and moved cross-cutting commentary under a `Design Notes` subsection.                                                            |
| PRF-SYS-010 | Added `recipe_tags` entity in Data Design View with component ownership (SYS-002/SYS-005), storage, transport/rest protection, and retention semantics.                                                                                        |
| PRF-SYS-011 | Updated dependency diagram to show SYS-016/SYS-017 client entry points to SYS-005 and authenticated client reachability to SYS-013.                                                                                                            |
| PRF-SYS-012 | Extended SYS-011 external-interface contract with explicit over-cap behavior: HTTP 422 `COLLECTION_CAP_EXCEEDED`; documented cap value as open item tied to REQ-049b TBD.                                                                      |
| PRF-SYS-013 | Added operational-state cross-reference annotations in interface contracts: SYS-006 `confirm` transition `PHOTO_PENDING → PHOTO_PROCESSING`; SYS-013 `202` ↔ `ERASURE_REQUESTED`, `410` ↔ `ERASURE_COMPLETED`.                                 |

## Glossary

| Term               | Definition                                                                                                                                          |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tombstone          | Soft-delete state (`deleted_at` set) where a recipe is hidden from normal read paths but retained for compliance/history.                           |
| Pending archive    | A replayable row in `recipe_version_pending_archives` representing a version not yet confirmed in S3.                                               |
| Substantive edit   | An edit touching ingredients or instructions, used as privacy-unlock criterion for imported public-source clones.                                   |
| Snapshot clone     | Collection clone that copies source membership at clone time and does not auto-sync afterward.                                                      |
| Pull-from-source   | User-initiated reconcile action that updates cloned collection membership under explicit add/remove/no-overwrite rules.                             |
| Idempotent erasure | `POST /v1/account/erasure` behavior that reuses queued/running jobs, returns 410 for completed jobs, and creates fresh jobs only after failure. |

## Revision History

| Date       | Author                                             | Changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-30 | AI Peer Review (Cycle 1, PRF-SYS-001..PRF-SYS-012) | Fixed ambiguous failure impact in SYS-002→SYS-003 dependency (PRF-SYS-001). Expanded error handling contracts for SYS-002→SYS-003 and SYS-011→SYS-012 internal interfaces (PRF-SYS-002, PRF-SYS-003). Added missing dependency rows SYS-011→SYS-014, SYS-011→SYS-001, SYS-012→SYS-014 (PRF-SYS-004, PRF-SYS-005, PRF-SYS-006). Corrected photo-lifecycle state ownership from SYS-008 to SYS-006/SYS-007 (PRF-SYS-007). Named erasure job table `account_erasure_jobs` (PRF-SYS-008). Added visibility-policy external interface surfacing note to SYS-002 (PRF-SYS-009). Updated dependency diagram to include SYS-008, SYS-011→SYS-001, and SYS-011→SYS-014 (PRF-SYS-010). Added coverage note on low-granularity requirements (PRF-SYS-011). Added cross-cutting infrastructure note to Derived Requirements (PRF-SYS-012).                                                                                                                                                                                                                          |
| 2026-05-01 | AI Peer Review (Cycle 2, PRF-SYS-001..PRF-SYS-013) | Updated Coverage Summary `87/87 → 129/129` with metric-definition note (Cycle 2 PRF-SYS-001). Confirmed REQ-NF-003/004 ownership (Cycle 2 PRF-SYS-002). Added atomic sub-ID traces REQ-035a/b, REQ-038a/b/c, REQ-049a/b (Cycle 2 PRF-SYS-003). Added dependency rows SYS-016/017→SYS-005, SYS-005→SYS-001, SYS-013→SYS-001 (Cycle 2 PRF-SYS-004). Added SYS-006→SYS-007 S3-event dependency (Cycle 2 PRF-SYS-005). Specified SYS-004 food-DB fallback contract (Cycle 2 PRF-SYS-006). Specified SYS-005 timeout/unavailability contract (Cycle 2 PRF-SYS-007). Added SYS-002→SYS-008 internal interface and SYS-018/019/020 scope note (Cycle 2 PRF-SYS-008). Added Derived Requirements / Design Notes split (Cycle 2 PRF-SYS-009). Added `recipe_tags` data entity (Cycle 2 PRF-SYS-010). Updated dependency diagram for client→search and client→erasure entry points (Cycle 2 PRF-SYS-011). Added SYS-011 over-cap interface contract (Cycle 2 PRF-SYS-012). Added operational-state cross-references in interface contracts (Cycle 2 PRF-SYS-013). |
| 2026-05-07 | AI Peer Review (Cycle 3, PRF-SYS-001..PRF-SYS-004) | Reconciled Revision History with Peer-Review Remediation Log (Cycle 3 PRF-SYS-001). Disambiguated `recipe_tags` ownership: Component column set to SYS-014 with SYS-002 (writer) / SYS-005 (reader) note (Cycle 3 PRF-SYS-002). Added configuration-ownership split note in Design Notes clarifying SYS-018 (provider) vs SYS-016/SYS-017 (consumers) for REQ-NF-018/019/020 verification routing (Cycle 3 PRF-SYS-003). Added coverage-regeneration note tying the `129 / 129` figure to `build-matrix.sh` and prohibiting hand-edits (Cycle 3 PRF-SYS-004).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-06 | Reconciliation (round 2)        | **D3** — Added `SYS-021 Home Widget Surface` (Decomposition, Dependency View, dependency diagram, external-interface home-layout row) tracing FR-046 (`REQ-058`/`REQ-058a…i` + `REQ-IF-006`, the 002-owned `PATCH /v1/profiles/me`); bumped component count 20→21 (Subsystem 7→8) and added a pending-Home coverage note; added the Home surface to the Overview domain list. **D1** — Rewrote SYS-007 to S3-only/non-VPC (no DB, ADR-0004) emitting SQS `photo-processed`; added the Fargate-consumer completion path (SYS-007→SYS-006 dependency + internal interface, SYS-006→SYS-014 write) that performs the `recipe_photos` completion `UPDATE`; reworded the `PHOTO_PROCESSING`/`PHOTO_FAILED` operational states and the S3-event interface accordingly. **D2** — Removed the local `users` data entity; `recipes.owner_id` documented as app-user ULID (from the token claim) (`VARCHAR NOT NULL`, no FK, no `users` table).                                                                                                                       |
