# Hazard Analysis (FMEA): Source-Agnostic Food Data Integration

**Feature Branch**: `003-usda-food-data`
**Created**: 2026-05-10
**Status**: Draft — **re-baselined 2026-06-22 to the source-agnostic food data model**
**Source**: `specs/003-usda-food-data/v-model/system-design.md`
**Standard**: General-Purpose FMEA (non-regulated software; `domain: ''` in `v-model-config.yml`)

> **Re-baseline note (2026-06-22).** This artifact (the V-Model V&V hazard layer; hazards trace to
> `requirements.md` REQ-\* ids and to the `system-design.md` SYS / `module-design.md` MOD mitigations) was
> regenerated to match the **source-agnostic food data redesign**. A food is now keyed by an internal
> surrogate `id` (ULID-valued, named `id`); **USDA is one pluggable source adapter** among many; foods are
> assembled into a **cross-source golden record** with per-field provenance; users add foods **by name**
> through a `PENDING → (UNRESOLVED) → RESOLVED` lifecycle (terminal `NOT_FOUND` / `FAILED`); the rate
> limiter is **per-source**. All `fdcId` / `fetch_status` / denormalized-nutrient-column references from the
> prior USDA-coupled design are removed from the canonical hazard view and **confined to the adapter-boundary
> hazards** (SYS-009/SYS-014, MOD-008; `fdcId → external_key` inbound), per REQ-046/SC-013.
>
> **Preserved (re-keyed) hazards.** HAZ-002, HAZ-003, HAZ-004, HAZ-005, HAZ-008, HAZ-014, HAZ-020,
> HAZ-021, HAZ-025, HAZ-026, HAZ-029, HAZ-030, the per-source limiter family (HAZ-011, HAZ-012, HAZ-015,
> HAZ-016, HAZ-041), the secret family (HAZ-024, HAZ-027, HAZ-028, HAZ-035), and the **auth slice
> (HAZ-036..HAZ-040)** keep their ids and substance — re-keyed `fdcId → id` and generalized from USDA-only
> to **per source**. **Recast hazards** (same id, new design): HAZ-001 (malformed `id`/empty name),
> HAZ-006 (`id` event payload), HAZ-007 (`food_id` `ON CONFLICT` dedup), HAZ-009 (change-driven refresh
> backlog), HAZ-010 (100-name batch cap), HAZ-013/HAZ-017/HAZ-019/HAZ-022/HAZ-023/HAZ-031/HAZ-032/HAZ-033/HAZ-034
> (re-pointed to the adapter boundary / crosswalk / merge engine). **New hazards introduced by the redesign:**
> HAZ-042..HAZ-047 (incoherent merge, wrong-merge, source poisoning, untrusted-data ingestion,
> add-by-name duplicate race, refresh clobbering a user resolution). No surviving HAZ id was renumbered.

## Overview

This document presents the Failure Mode and Effects Analysis (FMEA) for the **Source-Agnostic Food Data
Integration** feature. Every system component (`SYS-001`..`SYS-020`, including the auth slice
`SYS-013 AuthnAuthzLayer` and the seven new source-agnostic components `SYS-014`..`SYS-020`) from
`system-design.md` is assessed for realistic failure modes. Each hazard receives a unique `HAZ-NNN`
identifier and is linked to risk-control measures (`REQ-NNN` / `SYS-NNN` / `MOD-NNN`), enabling the
traceability chain: Hazard → Mitigation → Requirement → Test Case (Matrix H in `traceability-matrix.md`).

**Non-regulated context.** Commise is a consumer recipe management application. There are no life-safety,
vehicle-control, medical-device, or aviation-control concerns. Severity is measured against **user trust,
data integrity, privacy, availability, and platform cost** — not personal injury. Safety-critical
taxonomies (ISO 26262 ASIL, DO-178C DAL, IEC 62304) are intentionally **not** applied; `v-model-config.yml`
sets `domain: ''`.

## ID Schema

- **Hazard ID**: `HAZ-{NNN}` — 3-digit zero-padded, sequential (HAZ-001, HAZ-002, ...). Never renumbered.
- **Lineage**: From any `HAZ-NNN`, the Mitigation column lists `REQ-NNN`, `SYS-NNN`, and `MOD-NNN`
  references. The full chain to verification test cases (`ATP-NNN`, `STP-NNN`, `ITP-NNN`, `UTP-NNN`) lives
  in `traceability-matrix.md` (Matrix H — Hazard Traceability).
- **`fdcId` confinement (REQ-046/SC-013).** `fdcId` and USDA-native terms appear **only** in
  adapter-boundary hazards (SYS-009/SYS-014, MOD-008). Every other hazard is keyed on the internal ULID
  `id` and the source-agnostic `external_key`.

## Risk Matrix Definition

### Severity Scale (consumer SaaS — recipe app)

| Level        | Definition                                                                                                                                                                        |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Catastrophic | Cross-tenant data leak, broad unauthorized access, persistent loss of user-owned recipe data, or platform-wide outage.                                                            |
| Critical     | Individual-user data loss without recovery, sustained wrong-nutrition for a food across all users, security control bypass for one user, or sustained core-flow outage (≥1 hour). |
| Serious      | Recoverable degradation: failed resolution batch with retry path, single food stuck `UNRESOLVED`/`FAILED` with a recovery path, transient endpoint 5xx with idempotent retry.     |
| Minor        | Annoyance: slow food lookup turnaround, stale non-critical nutrition metadata, transient UI error with self-recovery.                                                             |
| Negligible   | Cosmetic only: log noise, telemetry drift, copy/wording inconsistency.                                                                                                            |

### Likelihood Scale

| Level      | Definition                                                                   |
| ---------- | ---------------------------------------------------------------------------- |
| Frequent   | Expected on a regular cadence under normal load (≥1× per day in production). |
| Probable   | Expected occasionally (≥1× per week per 1k MAU).                             |
| Occasional | Expected rarely (≥1× per month per 1k MAU).                                  |
| Remote     | Possible under unusual conditions (≥1× per quarter at small scale).          |
| Improbable | Conceivable only under stacked failure or adversarial conditions.            |

### Risk Level Matrix

|              | Frequent     | Probable     | Occasional   | Remote      | Improbable  |
| ------------ | ------------ | ------------ | ------------ | ----------- | ----------- |
| Catastrophic | Unacceptable | Unacceptable | Unacceptable | Undesirable | Undesirable |
| Critical     | Unacceptable | Unacceptable | Undesirable  | Undesirable | Tolerable   |
| Serious      | Unacceptable | Undesirable  | Undesirable  | Tolerable   | Tolerable   |
| Minor        | Undesirable  | Tolerable    | Tolerable    | Tolerable   | Acceptable  |
| Negligible   | Tolerable    | Tolerable    | Acceptable   | Acceptable  | Acceptable  |

**Disposition rule**: `Unacceptable` MUST be mitigated to `Undesirable` or lower before release.
`Undesirable` MUST have explicit residual-risk acceptance recorded in this document. `Tolerable`/`Acceptable`
are accepted as-is.

## Operational States

`system-design.md` defines queue and pipeline behavior but does not formally enumerate operational states;
the implicit state is **NORMAL** (steady-state production). The following operating modes are referenced
where relevant for state-dependent hazards:

| State              | Definition                                                                                                                       | Source                           |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| NORMAL             | Steady-state operation under expected load and per-source provider availability.                                                 | Implicit                         |
| DEGRADED-SOURCE    | A wired source (e.g. USDA) returns elevated latency, throttles, or 5xx; per-source retry/backoff active.                         | SYS-005, SYS-006, SYS-009        |
| RATE-LIMITED       | A source's rolling 60-min window is at/over its pause threshold (USDA ≥900/90%), `fetch_queue` row-lease release/backoff active. | SYS-005, SYS-006                 |
| BACKFILL-LAG       | Demand-weighted `fetch_queue` rows are accumulating backlog while user polling continues.                                        | SYS-003, SYS-004, SYS-005        |
| UNRESOLVED-BACKLOG | Foods sit `UNRESOLVED` awaiting a human candidate pick (`/candidates` + `PATCH`); the disambiguation queue grows.                | SYS-016, REQ-048/REQ-049         |
| REFRESH-WINDOW     | Change-driven refresh is comparing `food_sources.item_version` and re-enqueuing only changed-upstream items.                     | SYS-019, REQ-031/REQ-032/REQ-053 |

⚠️ No formally enumerated operational states exist in `system-design.md`. State-dependent severity is
captured with the implicit modes above; if `system-design.md` later introduces a formal state taxonomy,
this section MUST be reconciled.

## Hazard Register (FMEA)

> One or more `HAZ-NNN` per `SYS-NNN`. Mitigations cite existing `REQ-NNN`, `SYS-NNN`, and `MOD-NNN`
> identifiers from this feature only. `fdcId` appears **only** in the SYS-009/SYS-014 (MOD-008) adapter-boundary rows.

### SYS-001 — FoodApiController

| HAZ ID  | Component | Failure Mode                                                                                                       | Operational State | Effect                                                                      | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                              | Residual Risk |
| ------- | --------- | ------------------------------------------------------------------------------------------------------------------ | ----------------- | --------------------------------------------------------------------------- | -------- | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-001 | SYS-001   | Validation regression lets a malformed `id` (non-ULID) or empty/whitespace-only name into the create/enqueue path. | NORMAL            | `fetch_queue` pollution, spurious canonical rows, wasted per-source budget. | Serious  | Occasional | Undesirable | REQ-006 ULID + non-empty-name gate; MOD-001 `isValidUlid`/`normalizeName` early-return `400`; MOD-021 confirms no invalid input reaches the store.                      | Tolerable     |
| HAZ-002 | SYS-001   | API read path accidentally calls an external source on a local-store miss.                                         | NORMAL            | Request latency spikes; synchronous outage when the source is down.         | Critical | Remote     | Undesirable | REQ-001/REQ-009 local-store-only invariant; SYS-001 contract; MOD-001 reads via MOD-016 only, never an adapter in the request path.                                     | Tolerable     |
| HAZ-010 | SYS-001   | Batch add exceeds the 100-name cap and the over-limit request is partially enqueued instead of rejected.           | NORMAL            | Unbounded blast radius from one batch; per-item partial contract violated.  | Serious  | Remote     | Tolerable   | REQ-040a hard 100-name cap (`400`, enqueue nothing over cap); MOD-001 `handleBatch` length guard; the USDA ≤20-keys/call limit is an adapter-internal detail (REQ-023). | Tolerable     |

### SYS-002 — EventBridgeBus (scheduled producers + `FoodDataReceived` only)

| HAZ ID  | Component | Failure Mode                                                                                                      | Operational State | Effect                                                                             | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                       | Residual Risk |
| ------- | --------- | ----------------------------------------------------------------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------- | -------- | ---------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-004 | SYS-002   | Demand-path `fetch_queue` row insert / `pg_notify('fetch_queued', id)` fails and the add-by-name enqueue is lost. | DEGRADED-SOURCE   | Food remains `PENDING` indefinitely; stale user-visible data.                      | Critical | Remote     | Undesirable | REQ-011/REQ-014 Postgres-as-queue enqueue contract; MOD-002 enqueue-failure throw → `503`; MOD-003 periodic-poll fallback if `NOTIFY` is lost; SYS-012 alarms on enqueue errors. | Tolerable     |
| HAZ-006 | SYS-002   | `IngestionScheduled` / `FoodDataReceived` event payload schema drift (the food `id` field renamed/dropped).       | NORMAL            | Consumer discards events; scheduled refresh or completion fan-out silently stalls. | Serious  | Occasional | Undesirable | REQ-IF-005 event schema contract (all events carry the food `id`, never `fdcId`); MOD-002 payload guards; MOD-002 `CompletionEvent` typed shape.                                 | Tolerable     |

### SYS-003 — FetchQueue (single demand-weighted Postgres queue, keyed on food `id`)

| HAZ ID  | Component | Failure Mode                                                                                    | Operational State | Effect                                                                            | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                      | Residual Risk |
| ------- | --------- | ----------------------------------------------------------------------------------------------- | ----------------- | --------------------------------------------------------------------------------- | -------- | ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-003 | SYS-003   | Queue-grain dedup bypassed under concurrent enqueues for the same food `id`.                    | BACKFILL-LAG      | Duplicate `fetch_queue` rows and redundant fan-outs; per-source budget burn.      | Serious  | Occasional | Undesirable | REQ-013/REQ-014 queue-grain dedup; MOD-003 `INSERT … ON CONFLICT (food_id) DO UPDATE` collapses to one row; the food-`id` PK is the dedup key.                                  | Tolerable     |
| HAZ-005 | SYS-003   | Demand-weight / aging miscompute lets low-demand rows starve genuine user demand at drain time. | BACKFILL-LAG      | User-facing lookups drain behind low-demand backlog despite higher demand weight. | Serious  | Occasional | Undesirable | REQ-015 demand-weighted drain (`request_count DESC, first_requested ASC`) + aging; MOD-003 `leaseNext` ORDER BY with the >50-pending demotion tier folded in (REQ-039).         | Tolerable     |
| HAZ-007 | SYS-003   | `food_id` `ON CONFLICT` collision or lost row drops a distinct lookup request.                  | BACKFILL-LAG      | Some foods never fan out despite demand.                                          | Serious  | Remote     | Tolerable   | REQ-013/REQ-014 idempotent `ON CONFLICT (food_id)` keyed on the unique food `id` (collisions impossible across distinct `id`s); SYS-012 queue-depth alarms surface a stuck row. | Tolerable     |
| HAZ-021 | SYS-003   | A claimed `in_flight` row lease is orphaned after a worker crash and never reclaimed.           | BACKFILL-LAG      | Food stuck mid-resolution; API keeps returning `202`/`PENDING`.                   | Serious  | Occasional | Undesirable | REQ-017 single 30s `in_flight` lease; MOD-003 `leaseNext` reclaims rows where `lease_expires_at < now()`; SYS-012 stale-pending alarm.                                          | Tolerable     |

### SYS-005 — FoodFanOutMergeWorker (Fargate)

| HAZ ID  | Component | Failure Mode                                                                                          | Operational State | Effect                                                                      | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                   | Residual Risk |
| ------- | --------- | ----------------------------------------------------------------------------------------------------- | ----------------- | --------------------------------------------------------------------------- | -------- | ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- |
| HAZ-008 | SYS-005   | The single 30s `in_flight` lease is too short for the slowest source's latency profile.               | DEGRADED-SOURCE   | Premature reclaim creates duplicate fan-out work and backlog amplification. | Serious  | Occasional | Undesirable | REQ-017 lease configuration; MOD-004 per-source backoff before lease expiry; MOD-003 idempotent re-claim is safe (no double-resolve).                                                                        | Tolerable     |
| HAZ-011 | SYS-005   | A wired source outage or sustained 5xx collapses fan-out throughput for foods that need that source.  | DEGRADED-SOURCE   | Pending foods and refresh rows stall; user trust degrades.                  | Critical | Occasional | Undesirable | REQ-016/REQ-027 retry semantics (≤5 attempts, exponential backoff) then `FAILED` tombstone; MOD-004 per-source error branch; the fan-out resolves from the remaining wired sources where possible (REQ-050). | Tolerable     |
| HAZ-014 | SYS-005   | Fan-out/merge succeeds but the golden-record persist + queue-resolve transaction fails before commit. | NORMAL            | `PENDING` never clears; API repeatedly returns `202`.                       | Serious  | Occasional | Undesirable | REQ-024 transactional upsert; MOD-016 single-transaction `upsertGoldenRecord` + `resolve`; MOD-004 `requeueWithBackoff` on a failed write; SYS-012 alarms.                                                   | Tolerable     |

### SYS-006 — PerSourceRollingWindowLimiter

| HAZ ID  | Component | Failure Mode                                                                                                                                                                                                                                                                                                                                                                                                                               | Operational State | Effect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Residual Risk |
| ------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- |
| HAZ-012 | SYS-006   | A source's `429` despite the per-source rolling-window limiter (burst at the window edge / late-arriving timestamps).                                                                                                                                                                                                                                                                                                                      | RATE-LIMITED      | Requeue storm, delayed availability, increased cost.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Serious  | Occasional | Undesirable | REQ-019 per-source cap (USDA ≤1,000); MOD-005 per-source `source_call_log` window; MOD-004 `markWindowFull` + backoff branch on `429` (REQ-026).                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Tolerable     |
| HAZ-015 | SYS-006   | Trailing-window count math bug over-counts (or never ages out) a source's recent calls for extended periods.                                                                                                                                                                                                                                                                                                                               | RATE-LIMITED      | Throughput collapse for that source; backlog and stale data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Serious  | Occasional | Undesirable | REQ-019/REQ-020 per-source window constraints; MOD-005 deterministic trailing-60-min `count(*)` over `source_call_log` keyed by `source` with window-pruning tests.                                                                                                                                                                                                                                                                                                                                                                                                                                          | Tolerable     |
| HAZ-016 | SYS-006   | Atomic per-source windowed count+record race allows overshoot above a source's cap in any trailing 60-min window.                                                                                                                                                                                                                                                                                                                          | NORMAL            | Source quota breach (USDA >1,000), throttling/key sanctions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Critical | Remote     | Undesirable | REQ-018/REQ-020 hard per-source cap; MOD-005 single-statement check-and-record (`INSERT … SELECT WHERE (count(*) WHERE source=$1 …) < cap RETURNING`; Redis per-source sorted-set Lua deferred); REQ-022 single-instance worker.                                                                                                                                                                                                                                                                                                                                                                             | Tolerable     |
| HAZ-041 | SYS-006   | **Per-source** rolling-window **state loss** — a source's `source_call_log` partition is truncated, restored from an older snapshot, or otherwise reset, so that source's limiter restarts with windowCount = 0 while real calls were already made in the live trailing hour. Distinct from HAZ-015 (count/aging bug on intact state) and HAZ-016 (count+record race on intact state): here the persisted per-source state itself is lost. | RATE-LIMITED      | With the per-source count seeded at 0, the worker can fire up to ~that source's cap (USDA ~1,000) before the log refills to the real trailing-hour level — a **bounded burst** that briefly pushes that source's trailing-60-min total above its cap → **transient SC-002 breach for that source**. The breach is bounded and **self-converging**: as new timestamps accumulate, the window refills and the limiter re-converges to the cap on its own; no sustained overage, and other wired sources' windows are unaffected. | Critical | Remote     | Undesirable | **Durable committed per-source call log** — `source_call_log` writes commit in the same transaction as the per-source check-and-record (REQ-019/REQ-020; MOD-005 atomic count-and-insert) so each source's window survives worker/process restarts; **conservative startup** — on cold start or detected state loss, treat that source's window as full (or seed windowCount from recent `food_sources.fetched_at`) rather than 0. **Detection**: the per-source SC-002 monitor (trailing-60-min count alarm, SYS-012) surfaces the transient breach. Controls: REQ-019, REQ-020; SYS-006; SYS-012; MOD-005. | Tolerable     |

### SYS-007 — FoodCanonicalPostgresStore

| HAZ ID  | Component | Failure Mode                                                                                                  | Operational State | Effect                                                                                                                   | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                              | Residual Risk |
| ------- | --------- | ------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------ | -------- | ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-017 | SYS-007   | Crosswalk key drift — `food_sources (source, external_key)` rows attach the wrong food `id` to a source item. | NORMAL            | Wrong food attached to a source's data; ingredient references resolve to the wrong nutrition.                            | Critical | Remote     | Undesirable | REQ-028/REQ-029 `UNIQUE(source, external_key)` crosswalk invariant; MOD-006 `upsertCrosswalk` keyed `ON CONFLICT (source, external_key)`; the internal `id` is never a source key (REQ-045/REQ-CN-007). | Tolerable     |
| HAZ-018 | SYS-007   | Branded vs generic `kind` collision overwrites the canonical golden scalars for a similarly-named food.       | NORMAL            | Users see misleading nutrition for the selected ingredient.                                                              | Serious  | Occasional | Undesirable | REQ-028 `kind` (`generic\|branded`) golden scalar + normalized-name dedup key; MOD-006/MOD-016 keep each food a distinct `id`; merge writes scalars per food, not across foods (MOD-017).               | Tolerable     |
| HAZ-019 | SYS-007   | A tombstoned `NOT_FOUND`/`FAILED` food's `external_key` stays unresolved past the 30-day tombstone TTL.       | REFRESH-WINDOW    | Lookups return the tombstone for a food a source may have since added, until the TTL lapses and a later add re-attempts. | Serious  | Occasional | Undesirable | REQ-018/REQ-025 30-day tombstone TTL with re-attempt after lapse; MOD-006 `updateStatus(..., tombstonedAt)`; within TTL a `NOT_FOUND` returns `404` without re-enqueue.                                 | Tolerable     |

### SYS-008 — FoodRedisCache (deferred variant)

| HAZ ID  | Component | Failure Mode                                                                                                                                                                                                                          | Operational State | Effect                                                                                                          | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                            | Residual Risk |
| ------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | --------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-020 | SYS-008   | Cache invalidation missed after a successful golden-record upsert (**deferred-Redis variant only** — the lean Postgres default has no separate cache layer, so reads always reflect the upserted row and this hazard does not apply). | NORMAL            | Stale golden record served despite a newer merge in PostgreSQL (only when the deferred Redis cache is enabled). | Serious  | Occasional | Undesirable | REQ-030 `food:{id}` cache semantics; MOD-007 `DEL food:{id}` on upsert. _(N/A under the lean Postgres default; applies only if the deferred Redis cache is adopted.)_ | Tolerable     |

### SYS-009 / SYS-014 — Source adapter boundary (USDA — the _only_ `fdcId` surface)

> These are the **only** hazards where `fdcId` appears (REQ-046/SC-013). The adapter maps `fdcId →
external_key` inbound (MOD-008); a failure here is contained to the boundary and never leaks a source-native
> identifier into the canonical store.

| HAZ ID  | Component         | Failure Mode                                                                                | Operational State | Effect                                                       | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                            | Residual Risk |
| ------- | ----------------- | ------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------ | -------- | ---------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-013 | SYS-009 + SYS-014 | Malformed source nutrition payload is mapped and written with partial/incorrect macros.     | NORMAL            | Downstream nutrition planning uses corrupted values.         | Critical | Occasional | Undesirable | REQ-024/REQ-055 map-then-validate at the adapter; MOD-008 `fdcId → external_key` + canonical mapping; MOD-021 `validateAndSanitize` (type/range/length) reject-not-store before the value enters the store (SYS-020). | Tolerable     |
| HAZ-022 | SYS-009 + SYS-014 | USDA schema version drift renames/removes nutrition fields at the adapter boundary.         | DEGRADED-SOURCE   | Adapter mapping rejects payloads or maps the wrong nutrient. | Critical | Occasional | Undesirable | REQ-023/REQ-IF-004 USDA contract handling confined to MOD-008; REQ-055/MOD-021 strict field validation; a failed mapping rejects the candidate (the food resolves from other sources or lands `FAILED`).              | Tolerable     |
| HAZ-023 | SYS-009 + SYS-014 | Upstream metadata mismatch inverts a source item's `kind` (generic/branded) classification. | NORMAL            | Search ranking and ingredient matching quality degrade.      | Serious  | Occasional | Undesirable | REQ-024 typed canonical mapping in MOD-008; REQ-055/MOD-021 `kind ∈ {generic,branded}` validation; merge keeps `kind` per food (MOD-017).                                                                             | Tolerable     |

> Crosswalk-key drift (the wrong food `id` attached to a source item via `fdcId → external_key`) is the
> store-side concern registered once at **SYS-007 / HAZ-017**; SYS-014's contribution is the `fdcId →
external_key` mapping that HAZ-017's mitigation depends on (MOD-008). It is not double-counted here.

### SYS-010 — WebSocketNotificationLambda (deferred US-9)

| HAZ ID  | Component | Failure Mode                                                                  | Operational State | Effect                                                       | Severity | Likelihood | Risk Level | Mitigation                                                                                                                                             | Residual Risk |
| ------- | --------- | ----------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------ | -------- | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- |
| HAZ-025 | SYS-010   | Optional WebSocket notifier disabled but clients assume push-only completion. | NORMAL            | Users do not see completion without manual polling fallback. | Minor    | Occasional | Tolerable  | REQ-033 polling contract remains primary; REQ-034 marks WebSocket as optional/deferred.                                                                | Acceptable    |
| HAZ-026 | SYS-010   | `FoodDataReceived` push routed to the wrong connection/session.               | NORMAL            | Incorrect client state update; user confusion.               | Serious  | Remote     | Tolerable  | REQ-043 per-recipient delivery via the `fetch_requesters` subscription set (never broadcast); MOD-009 `id`-keyed correlation; SYS-012 structured logs. | Tolerable     |

### SYS-011 — SecretManagement (per-source API keys)

| HAZ ID  | Component | Failure Mode                                                                            | Operational State | Effect                                                                  | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                        | Residual Risk |
| ------- | --------- | --------------------------------------------------------------------------------------- | ----------------- | ----------------------------------------------------------------------- | -------- | ---------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-024 | SYS-011   | A source API key (e.g. the USDA key) is revoked/expired without timely rotation pickup. | DEGRADED-SOURCE   | That source's adapter cannot fetch; backlog grows for foods needing it. | Critical | Remote     | Undesirable | REQ-IF-006 per-source secret storage/rotation; MOD-010 per-source `getSourceApiKey(source)` + 5-min cache refresh; the fan-out still resolves from other wired sources (REQ-050). | Tolerable     |
| HAZ-027 | SYS-011   | A source secret value is logged/exposed through an error path or debug output.          | NORMAL            | Credential disclosure; potential source-API abuse and bill impact.      | Critical | Remote     | Undesirable | REQ-IF-006 secret non-exposure; REQ-044c only non-secret Clerk material on the request path; MOD-010 redacted errors; MOD-011 log-schema denylist.                                | Tolerable     |
| HAZ-028 | SYS-011   | Rotation occurs but a stale cached source key remains in the long-lived worker context. | DEGRADED-SOURCE   | Burst of source-auth failures until the cache TTL refresh.              | Serious  | Occasional | Undesirable | REQ-IF-006 rotation support; MOD-010 5-min TTL secret cache + refresh on auth failure.                                                                                            | Tolerable     |

### SYS-012 — MonitoringAndLogging

| HAZ ID  | Component | Failure Mode                                                                             | Operational State | Effect                                                       | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                          | Residual Risk |
| ------- | --------- | ---------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------ | -------- | ---------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-029 | SYS-012   | Queue-depth / per-source-window / `UNRESOLVED`-backlog alarms misconfigured or disabled. | BACKFILL-LAG      | Operations misses a prolonged incident; stale data persists. | Serious  | Occasional | Undesirable | REQ-NF-012/REQ-NF-016 observability requirements; MOD-011 EMF metrics (per-source call counts, UNRESOLVED backlog, tombstone count) + alarm wiring. | Tolerable     |
| HAZ-030 | SYS-012   | Metrics cardinality explosion from unbounded labels (e.g., raw query text or food name). | NORMAL            | CloudWatch cost spike, dropped high-cardinality telemetry.   | Minor    | Probable   | Tolerable   | REQ-NF-012 logging constraints; MOD-011 bounded EMF dimensions (no raw name/query as a dimension).                                                  | Acceptable    |

### SYS-013 — AuthnAuthzLayer

> Auth slice (US-0, FR-035–FR-053). The `AuthnAuthzLayer` (`SYS-013`, decomposed into `MOD-012
ClerkAuthMiddleware` + `MOD-013 DemotionAndFairness` + `MOD-014 AsyncProducerAuthz`) fronts **every**
> food data entry point (all HTTP routes + WebSocket `$connect`). These hazards capture the failure axis the
> rest of the register does not: an unauthenticated/forged caller reaching business logic (HAZ-036), an
> authenticated-but-abusive caller driving denial-of-wallet against a shared per-source budget (HAZ-037),
> the demand-state dependency the per-`sub` demotion scorer rests on (HAZ-038), confusion between the two
> token classes the same verifier accepts (HAZ-039), and the one cacheable authorizer surface — WebSocket
> `$connect` — that can fall open (HAZ-040). Re-keyed from `fdcId` to the food `id`; USDA-only budget
> language generalized to **per source**.

| HAZ ID  | Component | Failure Mode                                                                                                                                                                                                                                                        | Operational State | Effect                                                                                                                                                                                                                                                                                                                               | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Residual Risk |
| ------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-036 | SYS-013   | Unauthenticated / auth-bypass access — anonymous, forged-identity-header, expired, or wrong-`azp`/wrong-instance caller reaches food endpoints or async producers.                                                                                                  | NORMAL            | Anonymous read of the service and, worse, unauthenticated callers (sync HTTP edge **or** async EventBridge/`fetch_queue` producers) drive rate-limited per-source spend / data access with no attributable principal.                                                                                                                | Critical | Occasional | Undesirable | REQ-035/REQ-037a networkless Clerk `verifyToken` (`CLERK_JWT_KEY` + `azp`) fail-closed `401` on every entry point; REQ-037c identity from verified `sub` only (no client-suppliable header, mirrors PR #39); REQ-037d fail-closed default; REQ-043 WebSocket `$connect` auth (pinned `403`); REQ-041 M2M token class; REQ-042 named least-privilege async producers + consumer provenance validation; REQ-IF-008. Controls: SYS-013; MOD-012 (verify + `401`) and MOD-014 (provenance).                                                                           | Tolerable     |
| HAZ-037 | SYS-013   | Insider denial-of-wallet — a valid authenticated `sub` exhausts a shared per-source budget (USDA 1,000 req/hr) or starves the queue via `request_count` inflation / oversized batches.                                                                              | RATE-LIMITED      | One legitimate account scripting lookups consumes a source's budget and starves all other users; or repeat single-`sub` adds pin one food `id` to the queue front. Exhausting a hard per-source budget is a **sustained outage of the core fetch flow for ALL users** — Critical by the severity scale here.                         | Critical | Occasional | Undesirable | REQ-039 fairness **by demotion, not rejection** — no per-`sub` `429`: a `sub` with **>50 items pending** is ranked to the **back** of the drain order (dynamic, at drain time, work-conserving, SC-012); REQ-019 per-source rolling-window pause at 90% is the hard budget guarantee; REQ-039 distinct-requester demand counting (`fetch_requesters`, cap=1) with aging; REQ-040a 100-name batch cap (`400`); REQ-040b `fetch_queue` depth + circuit breaker (`503`); REQ-044a auth-layer load-shed. Controls: SYS-013; MOD-013 (demotion/fairness/backpressure). | Tolerable     |
| HAZ-038 | SYS-013   | Demotion-scorer demand-state unavailability — the per-`sub` demotion (REQ-039) reads the requester's current pending count from `fetch_queue` + `fetch_requesters` at drain time; that read is unavailable or times out, forcing a fail-open vs fail-closed choice. | demand-state-down | If the scorer fails **closed** (treats every `sub` as over-50 / rejects on a transient blip) it converts a temporary read failure into a starvation/`503` outage for legitimate users; if it fails **open** (don't demote), the only consequence is temporary loss of inter-user fairness. The hazard is choosing the wrong default. | Critical | Occasional | Undesirable | **Fail-open to availability**: when the per-`sub` pending count is unavailable, the scorer does **not** demote (and never rejects an authenticated request). Safe because the per-source rolling-window cap (REQ-019) remains the hard budget guarantee independent of demotion, so a read failure can only cause temporary unfairness, never a budget breach. Controls: REQ-039; SYS-013; MOD-013 (fail-open-to-availability).                                                                                                                                   | Tolerable     |
| HAZ-039 | SYS-013   | Token-class confusion (user vs M2M) — REQ-041/A-012 introduce two token classes verified by the **same** networkless path and `azp` allowlist, so a token valid for one class can be accepted on a surface intended for the other.                                  | NORMAL            | A valid M2M token opening a user-only WebSocket `$connect`, or a user session token reaching a service/operational surface, is a privilege-boundary failure even though no token is forged.                                                                                                                                          | Critical | Remote     | Undesirable | Per-endpoint **token-class classification** (REQ-041: every endpoint user-token / service-token / both) enforced after verification, plus scope checks (REQ-038b/REQ-038c authz `403` precedence). The `AuthenticatedCaller` carries class/`azp` so the wrong class is rejected (`401`/`403`). Controls: REQ-041, REQ-IF-008; SYS-013; MOD-012 (verify + class/scope).                                                                                                                                                                                            | Tolerable     |
| HAZ-040 | SYS-013   | WebSocket `$connect` authorizer cache fall-open — the deferred WS `$connect` Lambda authorizer (REQ-043) is the **one** surface with an API Gateway authorizer result cache, which can replay a once-valid policy after the token expired/was revoked.              | NORMAL            | A cached "allow" keyed on a client-controllable value, or a non-zero TTL, lets a connection re-establish on an expired/revoked token. Because the HTTP API uses the in-process `AuthMiddleware` with no cache, this replay window exists only at `$connect`.                                                                         | Critical | Remote     | Undesirable | REQ-043: WebSocket authorizer caching disabled (**TTL = 0**) or keyed **solely on the verified token**; authorizer attached to **every** WS route AND method; denied auth returns `401`/`403`, never default-open; mid-connection expiry closes/re-auths. Controls: REQ-043, REQ-IF-008; SYS-013; MOD-012 (`$connect` verification).                                                                                                                                                                                                                              | Tolerable     |

### SYS-014 — SourceAdapterRegistry (new) — see SYS-009/SYS-014 boundary block above

> SYS-014's adapter-boundary failure modes (malformed mapping, schema drift, `kind` inversion, the
> `fdcId → external_key` mapping) are registered jointly with SYS-009 in the **Source adapter boundary**
> block (HAZ-013, HAZ-022, HAZ-023). Registry-level misconfiguration (duplicate/unknown source, empty
> registry) fails closed at boot per MOD-015 and is not a runtime hazard.

### SYS-015 — GoldenRecordMergeEngine (new)

| HAZ ID  | Component | Failure Mode                                                                                                                                                                                                                                                                                 | Operational State | Effect                                                                                                                                                                                                                                | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                       | Residual Risk |
| ------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-042 | SYS-015   | **Nutritionally-incoherent merge** — the merge blends nutrient values across sources on **mismatched bases** (one source per-serving, another per-100g) without normalizing first, producing a golden record whose macros are internally inconsistent.                                       | NORMAL            | A `RESOLVED` food carries silently wrong nutrition (e.g. a per-serving value blended as if per-100g), corrupting every downstream nutrition/meal-plan calculation for all users of that food — a sustained wrong-nutrition condition. | Critical | Occasional | Undesirable | REQ-051/REQ-NF-018 **per-100g normalization BEFORE any cross-source blend** — MOD-017 `normalizeToPer100g` runs per nutrient first, conflicts then resolved to the higher-priority source with `food_nutrients.source_id` recording the winner; a value that cannot be normalized to per-100g (per-serving with no serving grams) is dropped (presence→absence), never blended at the wrong basis (MOD-017 error table, SC-008). | Tolerable     |
| HAZ-044 | SYS-015   | **Source-data poisoning / "longer-wins" amplification** — a low-quality or adversarial source supplies an inflated/garbage free-text field (`description`/`ingredients`) that wins purely by being longest, or supplies identity fields (`name`/`brand`) that displace a trustworthy source. | NORMAL            | Misleading or junk free-text in the golden record; or a malicious source overriding identity fields and steering the food's identity.                                                                                                 | Serious  | Remote     | Tolerable   | REQ-051 identity/short fields (`name`, `brand`) take the **higher-priority source** (NOT longest) — MOD-017 `highestPriorityWithValue` + MOD-015 static `PRIORITY_ORDER` (USDA default highest); longer-wins applies only to free-text, and REQ-055/MOD-021 length caps + text sanitization bound how much a source can inject before it reaches the merge (SYS-020).                                                            | Tolerable     |

### SYS-016 — CandidateResolutionService (new)

| HAZ ID  | Component         | Failure Mode                                                                                                                                                                             | Operational State  | Effect                                                                                                                                                                       | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Residual Risk |
| ------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-043 | SYS-015 + SYS-016 | **Wrong-merge** — two genuinely distinct foods are auto-unified into one golden record (over-eager pre-merge collapse), or a resolve picks a candidate that does not belong to the food. | UNRESOLVED-BACKLOG | A `RESOLVED` food conflates two different items' nutrition — one wrong record served confidently to all users; or cross-food contamination via an out-of-set candidate pick. | Critical | Occasional | Undesirable | REQ-050 **confident-collapse-only** auto-resolve — MOD-017 `confidentlyCollapsible` returns `UNRESOLVED` on residual ambiguity rather than guessing; REQ-048/REQ-049 user disambiguation (`UNRESOLVED` → `/candidates` → `PATCH`) makes the human the final arbiter (MOD-018); REQ-049 candidate-set membership validation rejects an out-of-set pick (`400`/`409`, `status` unchanged) so a resolve cannot contaminate another food. Controls: REQ-048, REQ-049, REQ-050, REQ-051; SYS-015, SYS-016; MOD-017, MOD-018. | Tolerable     |

### SYS-018 — FoodDaoRepositoryLayer (new)

| HAZ ID  | Component | Failure Mode                                                                                                                                                                                                     | Operational State | Effect                                                                                                                                 | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                                      | Residual Risk |
| ------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-046 | SYS-018   | **Concurrent add-by-name duplicate-food race** — two simultaneous `POST /v1/foods` for the same name both miss the existence check and create two canonical rows + `id`s for one logical food (thundering herd). | NORMAL            | Duplicate foods, split provenance/candidate sets, duplicate per-source fetches and budget burn, ambiguous ingredient links downstream. | Serious  | Occasional | Undesirable | REQ-005/REQ-047 normalized-name dedup under a short **advisory lock** — MOD-016 `createByName` takes `pg_advisory_xact_lock(hash(normalized_name))`, re-checks, and collapses concurrent adds to one `id`; the `UNIQUE(normalized_name)` index is the durable backstop if the lock is skipped (MOD-016 error table); the food-`id` PK + `ON CONFLICT` then dedups the enqueue (REQ-013). Controls: REQ-005, REQ-013, REQ-047; SYS-018; MOD-016. | Tolerable     |

### SYS-019 — ChangeDrivenRefresh (new)

| HAZ ID  | Component | Failure Mode                                                                                                                                                                                                           | Operational State | Effect                                                                                                                                                                                                       | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Residual Risk |
| ------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-009 | SYS-019   | Change-driven refresh re-enqueues (low-demand `fetch_queue` rows) accumulate and never drain under sustained high demand-weighted load (or a source stays down for days).                                              | REFRESH-WINDOW    | Nutrition data ages beyond freshness target; the read still serves the already-held record (availability over freshness — no max-staleness cutoff), so the effect is degraded freshness, not unavailability. | Serious  | Occasional | Undesirable | REQ-031 no max-staleness cutoff (reads never block on source health); REQ-053 change-driven refresh; REQ-015 demand-weighted drain so high-demand rows are not starved by refresh rows; MOD-020 re-enqueues only changed-upstream items as low-priority; SYS-012 queue-depth alarms.                                                                                                                                                                                                                            | Tolerable     |
| HAZ-047 | SYS-019   | **User-resolution clobbered by refresh** — a background refresh blindly re-blends and overwrites a field the user manually resolved (REQ-049), or overwrites an unchanged stored value, discarding the human decision. | REFRESH-WINDOW    | A user's deliberate disambiguation/correction is silently lost on the next refresh cycle; loss of trust and of human-curated data.                                                                           | Serious  | Occasional | Undesirable | REQ-031/REQ-053 **change-driven refresh that preserves stored/user values** — MOD-020 re-pulls a field **only** when its originating source item's `food_sources.item_version` changed upstream, never blindly re-blends; the user's pick is stored as **ordinary provenance** (MOD-018/MOD-019), so only its originating external item changing can move it; unchanged fields (incl. user-resolved) are left intact (MOD-020 `LeavingIntact`). Controls: REQ-031, REQ-053; SYS-019; MOD-020, MOD-018, MOD-019. | Tolerable     |

### SYS-020 — AdapterInputValidation (new)

| HAZ ID  | Component | Failure Mode                                                                                                                                                                                                                                | Operational State | Effect                                                                                                                                                                           | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                                                                                                                                                                                                                    | Residual Risk |
| ------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-045 | SYS-020   | **Untrusted external data stored unvalidated** — a source response (malformed, oversized, control-char/markup-laden, out-of-range nutrient, or delivered over a non-HTTPS/cert-invalid channel) is mapped and persisted without validation. | NORMAL            | Canonical store poisoned with malformed/oversized/injected values; or a tampered-in-transit payload trusted as authentic, corrupting golden records and downstream calculations. | Critical | Remote     | Undesirable | REQ-055 **adapter-boundary validation + transport security** — MOD-021 `validateAndSanitize` (type/range/length caps, control-char/null-byte stripping, `kind`/`basis` enums) **reject-not-store** on failure, and `assertHttps` refuses non-HTTPS / cert-failed fetches before any value enters the store; complements SYS-001's request-edge validation. Controls: REQ-055, REQ-024, REQ-NF-018; SYS-020; MOD-021, MOD-008. | Tolerable     |

## Progressive Deepening Addendum (Architecture-/Module-level Hazards)

The following hazards emerged from `architecture-design.md` / `module-design.md` decomposition and capture
failure modes only visible at the module boundary (interface mismatches, key-serialization, normalization
collisions, secret-in-telemetry). They are **appended** to the SYS-level register above.

| HAZ ID  | Component         | Failure Mode                                                                                                                             | Operational State | Effect                                                                             | Severity | Likelihood | Risk Level  | Mitigation                                                                                                                                                                                                                          | Residual Risk |
| ------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------- | -------- | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| HAZ-031 | MOD-004 + MOD-016 | Golden-record upsert succeeds but the lifecycle status write (`PENDING`→`RESOLVED`/`UNRESOLVED`) is skipped.                             | NORMAL            | Food data present but logically still `PENDING`; API keeps returning `202`.        | Serious  | Remote     | Tolerable   | MOD-016 single-transaction `upsertGoldenRecord` writes scalars + status atomically; REQ-002/REQ-003 status semantics validated in tests.                                                                                            | Tolerable     |
| HAZ-032 | MOD-006 + MOD-007 | Cache/DB key serialization mismatch for the internal `id` (`food:{id}` formatting drift, deferred Redis variant).                        | NORMAL            | Duplicate cache entries, stale reads, invalidation misses (deferred variant only). | Serious  | Occasional | Undesirable | REQ-030 canonical `food:{id}` ULID key; MOD-007 single `FOOD_KEY(id)` formatter; N/A under the lean Postgres default.                                                                                                               | Tolerable     |
| HAZ-033 | MOD-021 + MOD-017 | Multilingual / fuzzy food-name normalization collision pre-merges two distinct foods (the wrong-merge entry-point at the matcher grain). | NORMAL            | Search-quality degradation; feeds the HAZ-043 wrong-merge path.                    | Serious  | Occasional | Undesirable | REQ-050 confident-collapse-only (MOD-017 `confidentlyCollapsible` → `UNRESOLVED` on doubt); REQ-008 relevance ranking; MOD-021 locale-safe text sanitization; ultimate backstop is human disambiguation (HAZ-043, REQ-048/REQ-049). | Tolerable     |
| HAZ-034 | MOD-006 + MOD-001 | Search-ranking poisoning via adversarial query/token distribution in the local trigram index.                                            | NORMAL            | Low-quality or misleading foods dominate top search results.                       | Serious  | Remote     | Tolerable   | REQ-010 deterministic relevance ranking; MOD-006 bounded `pg_trgm` similarity scoring; SYS-012 anomaly monitoring.                                                                                                                  | Tolerable     |
| HAZ-035 | MOD-011 + MOD-010 | Logging/tracing path records secret-bearing upstream headers (per-source API key).                                                       | NORMAL            | Security leakage into the telemetry sink.                                          | Critical | Remote     | Undesirable | REQ-IF-006 + REQ-044c; MOD-010 redaction and MOD-011 log-schema denylist for auth/source-key headers.                                                                                                                               | Tolerable     |

## Coverage Summary

| Metric                                                 | Value  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Total SYS components analyzed                          | 20     | `SYS-001`..`SYS-020` from `system-design.md` (incl. the auth slice `SYS-013` and the seven new source-agnostic components `SYS-014`..`SYS-020`).                                                                                                                                                                                                                                                                                                                                                                    |
| SYS-level hazards (preserved/recast, HAZ-001..HAZ-030) | 30     | The HAZ-001..HAZ-030 register, re-keyed `fdcId → id` and USDA-only → per-source; `fdcId` confined to HAZ-013/017/022/023 (adapter boundary). Old-schema-only framings (single-USDA window, `fetch_status` lifecycle, denormalized-nutrient column, stale-by-age refresh) were **recast** onto the new design on the same ids; none renumbered.                                                                                                                                                                      |
| Per-source rolling-window state-loss hazard (HAZ-041)  | 1      | `SYS-006`: per-source `source_call_log` partition reset → that source's windowCount restarts at 0 → bounded, self-converging burst → transient SC-002 breach for that source. Distinct from HAZ-015/HAZ-016.                                                                                                                                                                                                                                                                                                        |
| Auth-slice hazards (HAZ-036..HAZ-040)                  | 5      | `SYS-013` (US-0, FR-035–FR-053): HAZ-036 auth-bypass, HAZ-037 insider denial-of-wallet (per-source), HAZ-038 demotion-scorer fail-open, HAZ-039 token-class confusion, HAZ-040 WS `$connect` cache fall-open. Preserved; re-keyed.                                                                                                                                                                                                                                                                                  |
| **New redesign hazards (HAZ-042..HAZ-047)**            | **6**  | HAZ-042 incoherent merge (per-100g normalization, REQ-051/MOD-017); HAZ-043 wrong-merge (user-disambiguation + confident-collapse-only, REQ-048/049/050, MOD-017/018); HAZ-044 source poisoning/longer-wins (priority-by-source + sanitization, REQ-051/055, MOD-017/021); HAZ-045 untrusted-data ingestion (boundary validation + HTTPS, REQ-055/MOD-021); HAZ-046 add-by-name duplicate race (advisory lock, REQ-047/MOD-016); HAZ-047 refresh clobbers user resolution (change-driven refresh, REQ-053/MOD-020). |
| Architecture/module-level hazards (HAZ-031..HAZ-035)   | 5      | Appended via Progressive Deepening; HAZ-033 recast as the matcher-grain wrong-merge entry-point feeding HAZ-043.                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Total hazards**                                      | **47** | Range `HAZ-001`..`HAZ-047` (no id renumbered). HAZ-013/017/022/023 are the only `fdcId`-bearing rows (adapter boundary).                                                                                                                                                                                                                                                                                                                                                                                            |
| Hazards at `Unacceptable` risk level                   | 0      | All hazards mitigated to `Undesirable` or lower before this register was finalized.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Hazards at `Undesirable` risk level                    | 39     | All carry explicit mitigation; residual-risk acceptance recorded inline (incl. HAZ-036..HAZ-047).                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Hazards at `Tolerable` risk level                      | 8      | Accepted as-is or accepted after mitigation (HAZ-007, HAZ-010, HAZ-025, HAZ-026, HAZ-030, HAZ-031, HAZ-034, HAZ-044).                                                                                                                                                                                                                                                                                                                                                                                               |
| Hazards at `Acceptable` residual risk                  | 2      | HAZ-025 (WS-disabled fallback) and HAZ-030 (telemetry cardinality) accepted as-is; the other 45 sit at `Tolerable` residual.                                                                                                                                                                                                                                                                                                                                                                                        |
| Mitigations referencing `REQ-NNN`                      | ✓      | Every hazard mitigation cites at least one `REQ-NNN` plus its `SYS/MOD` companion control.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `fdcId` confinement (REQ-046/SC-013)                   | ✓      | `fdcId` appears only in the SYS-009/SYS-014 (MOD-008) adapter-boundary hazards (HAZ-013, HAZ-017, HAZ-022, HAZ-023).                                                                                                                                                                                                                                                                                                                                                                                                |
| `[HUMAN REVIEW REQUIRED]` flags                        | 0      | None — every SYS has at least one realistic failure mode.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

> **Count reconciliation.** The register holds **47** distinct active hazards (HAZ-001..HAZ-047, contiguous,
> no gaps): HAZ-001..HAZ-035 = 35 SYS-/module-level (preserved or recast), HAZ-036..HAZ-040 = 5 auth slice,
> HAZ-041 = per-source state loss, HAZ-042..HAZ-047 = 6 new redesign hazards. Re-baselining stripped the
> old-schema-only framings (single-USDA rolling window, `fetch_status` lifecycle, denormalized-nutrient
> columns, stale-by-age refresh) by **recasting** their ids onto the new design rather than retiring ids, so
> no id was removed or renumbered. The crosswalk-drift cross-reference under the adapter-boundary block is a
> pointer to HAZ-017, not a separate hazard, and is not counted.

## Domain Note (non-regulated)

This analysis intentionally omits regulated-domain artifacts (ASIL classification, DAL assignment, MC/DC
obligations, IEC 62304 software safety classification). `v-model-config.yml` declares `domain: ''`
(consumer SaaS); reintroduce regulated taxonomies only if Commise enters a regulated domain. Severity is
framed against user trust, data integrity, privacy, availability, and platform cost — the only failure axes
that apply to a consumer recipe application.

## Glossary

| Term                      | Definition                                                                                                                        |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| FMEA                      | Failure Mode and Effects Analysis — structured enumeration of failure modes, effects, and mitigations per component.              |
| Hazard                    | A realistic failure mode whose effect, if unmitigated, would breach a system invariant (data integrity, privacy, etc.).           |
| Golden record             | The single cross-source canonical food assembled by field-level merge with per-field provenance (SYS-015/SYS-017).                |
| Wrong-merge               | Two genuinely distinct foods unified into one record, or an out-of-set candidate resolving a food (HAZ-043).                      |
| Incoherent merge          | Nutrient values blended across mismatched bases (per-serving + per-100g) without normalization (HAZ-042).                         |
| Operational state         | A system mode in which a hazard occurs (NORMAL, DEGRADED-SOURCE, RATE-LIMITED, BACKFILL-LAG, UNRESOLVED-BACKLOG, REFRESH-WINDOW). |
| Risk control / mitigation | Existing design control (`REQ/SYS/MOD`) that reduces hazard probability and/or impact.                                            |
| Residual risk             | Remaining risk after listed mitigations are in place.                                                                             |
| Add-by-name dedup         | Normalized-name advisory lock collapsing concurrent adds of the same food to one `id` (HAZ-046, MOD-016).                         |
| `fdcId` confinement       | The invariant that `fdcId`/USDA terms appear only at the adapter boundary (MOD-008), mapped to `external_key` (REQ-046).          |
| Matrix H                  | Hazard Traceability matrix in `traceability-matrix.md` connecting hazards → controls → verification cases.                        |
