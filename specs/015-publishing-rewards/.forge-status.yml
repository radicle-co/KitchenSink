# Product Forge status — 015 Publishing Rewards
# SEEDED 2026-08-22, not generated by a Phase 1 run.
#
# ⚠️ Phases 1–4 are marked complete because this feature's specification was
# authored through /speckit-specify + a dedicated research pass, NOT through the
# Product Forge research → product-spec → revalidate → bridge chain.
# Phase 4 (bridge) WRITES spec.md. Running it here would regenerate a 47-FR
# specification from a product-spec/ that does not exist. It MUST NOT run.
schema_version: 3
feature_name: 'Publishing Rewards'
feature_dir: 'specs/015-publishing-rewards'
feature_mode: 'standard'
flow_mode: 'gated'
created: '2026-08-22'

phases:
    problem_discovery:
        status: 'not_applicable'
    research:
        status: 'completed'
        note: 'Satisfied by research/reward-psychology.md (behavioural + legal evidence base) and docs/competitive/ (ReciMe teardown + gap analysis). Not a Product Forge research/ run.'
    product_spec:
        status: 'not_applicable'
        note: 'Bypassed. Requirements were authored directly as spec.md via /speckit-specify.'
    design_system_harvest:
        status: 'not_applicable'
    revalidation:
        status: 'completed'
        note: 'Satisfied by checklists/requirements.md iterations 1-4 and owner clarifications C-015-001..020.'
    bridge:
        status: 'not_applicable'
        note: '⛔ MUST NOT RUN. spec.md already exists and is authoritative. Bridge would overwrite it.'
    i18n_harvest:
        status: 'pending'
        note: 'Conditional. Project is multi-locale; FR-024 requires localization. Evaluate after plan.'
    plan:
        status: 'completed'
        digest_path: 'specs/015-publishing-rewards/plan/digest.md'
    tasks:
        status: 'completed'
        digest_path: 'specs/015-publishing-rewards/tasks/digest.md'
        total: 49
        groups: 9
        test_first: 21
        requirement_coverage: '47/47'
        blocked_requirements: 15
    migration_plan:
        status: 'completed'
        note: 'Triggered by plan.md data model. forward/rollback/validation SQL written and EXECUTED against PG16 and PG18.6 throwaway DBs. Found and fixed a non-working ratchet CHECK; found a table-name collision and a duplicated rating aggregate. Carries one owner decision: §3 grandfathering — reframed 2026-08-22 from a capability problem to an EQUITY one (the publish-to-earn path exists for everyone, and status=draft is an un-gated private-in-effect state), so Option C do-nothing is now the default.'
    pre_impl_review:
        status: 'pending'
    implement:
        status: 'blocked'
        note: '⛔ STOP LINE (owner directive 2026-08-22): do not implement. Feature is launch-blocked on D4a/D4b, D5, D24 and the missing ToS content licence.'
    code_review:
        status: 'pending'
    verify:
        status: 'pending'
    test_plan:
        status: 'pending'
    test_run:
        status: 'pending'
    release_readiness:
        status: 'pending'
    monitoring_setup:
        status: 'pending'
    experiment_design:
        status: 'pending'
    spec_merge:
        status: 'pending'

gates:
    - phase: 'research'
      decision: 'approved'
      mode: 'seeded'
      note: 'Owner reviewed the evidence base in-session and answered Q1-Q3 (C-015-017..019).'
    - phase: 'revalidation'
      decision: 'approved'
      mode: 'seeded'
      note: 'Checklist iteration 4 validated; 0 NEEDS CLARIFICATION markers remain.'

scope:
    paths:
        - 'packages/services/recipe-service'
        - 'packages/apps/commise/web'
        - 'packages/apps/commise/mobile'
        - 'packages/schemas/recipe'
    cross_workspace: true

stop_line:
    phase: 'implement'
    reason: 'Owner directive 2026-08-22 — stop after tasks. Feature is launch-blocked on 016 (ToS licence + DMCA), D4a, D5; and US5/US6 are unbuildable until 008 and 012 exist.'
last_updated: '2026-08-22'
